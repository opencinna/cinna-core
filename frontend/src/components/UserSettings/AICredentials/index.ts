/**
 * The folder keeps the module specifier `@/components/UserSettings/AICredentials`
 * that `routes/_layout/settings.tsx` already imports, so the split from a
 * single 1027-line file did not have to touch the route.
 */
export { AICredentialsSettings } from "./AICredentialsSettings"
