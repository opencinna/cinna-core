// Reuse the generic webhook token reveal UI from the task-triggers feature.
// It already implements the red/amber-outline banner + three copy blocks
// (URL / token / curl example) per the project's webhook UX convention.
export { WebhookTokenDisplay } from "@/components/Tasks/Triggers/WebhookTokenDisplay"
