# Entrypoint prompt

<!-- A short, human-like first message used to start an UNATTENDED run (a schedule
     or a trigger). Nobody is present to fill anything in, so it must be entirely
     self-contained: no placeholders, no brackets, no "the user's" anything.
     One or two conversational sentences. Required once the agent has a schedule. -->

Run today's check and report what needs attention.
