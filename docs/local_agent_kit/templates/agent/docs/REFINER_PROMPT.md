# Refiner prompt

<!-- Instructions for turning a vague request into a structured task before
     execution: default-fill rules and mandatory fields. Optional; fill it in when
     requests to this agent are routinely under-specified. -->

If no period is given, default to the current week.
Always capture the entity the request refers to; if it is missing and cannot be
defaulted, ask for it before starting.
