# Cinna Core

A conversational AI agent platform where users create custom AI agents, run them in isolated Docker environments, and interact through persistent chat sessions. Agent developers publish versioned bundles to a catalog; other users install them. Agents can be scheduled, triggered by email/webhooks, and exposed via A2A and MCP protocols.

**Stack:** FastAPI + PostgreSQL | React + TypeScript + TanStack | Docker isolation | SQLModel ORM

## Core Idea

The platform separates three distinct layers:

- **Agent** (logical definition) — what the agent does: custom prompts, credentials, SDK configuration
- **Environment** (runtime instance) — where it runs: a Docker container with the agent's workspace, tools, and files
- **Session** (conversation) — how users interact: a persistent chat thread with independent message history

One agent can have multiple environments (for testing, production, or rollback via blue-green deployment). One environment can host multiple sessions that share the same file workspace but maintain separate conversation histories.

Agents operate in two modes:
- **Building mode** — development state; agent uses a larger context window, can create/modify scripts and configure integrations
- **Conversation mode** — execution state; agent runs pre-built workflows with a lightweight prompt for faster, cheaper responses

Sessions can be started manually, by automated triggers (CRON, email, webhook), or by other agents via handover. External systems can connect through A2A (Agent-to-Agent protocol) or MCP (Model Context Protocol).

---

## Glossary

| Term | Definition |
|------|-----------|
| **Agent** | User-defined AI assistant with custom prompts, credentials, and SDK configuration |
| **Agent Environment** | Runtime instance (Docker container or remote server) where an agent executes |
| **Session** | Persistent chat conversation between a user and an agent environment |
| **Message** | Single communication unit within a session (user or agent) |
| **Activity** | Logged event or summary of agent actions within a session |
| **Credential** | Encrypted API key or service account used by agents (e.g., Gmail, Odoo) |
| **AI Credential** | LLM provider API key (Claude, OpenAI, etc.) for agent runtime |
| **Knowledge Source** | Git-based repository of documentation agents can query via RAG |
| **Agent Plugin** | Marketplace capability that extends agent functionality |
| **Input Task** | User-submitted task that goes through refinement before agent execution; extended with short-code IDs, comments, attachments, status history, and subtask delegation |
| **Task Trigger** | Automated rule (CRON, webhook, date) that creates tasks for an agent |
| **Agent Bundle** | Publisher-owned, versioned packaging of an agent identified by a reverse-DNS bundle ID. Publishing an agent creates a bundle; each publish creates an immutable revision |
| **Bundle Revision** | Immutable snapshot of a bundle's workspace content (scripts, docs, knowledge, files, prompts, SDK settings, agent schedules, agent-row metadata: description, example prompts, status refresh command, agent API / A2A / webapp enablement flags, SDK config, and the full A2A capability contract) taken at publish time |
| **Install** | A user's running copy of a published bundle — an `Agent` row seeded from a bundle revision with its own persistent App Data volume |
| **App Data** | Per-user, per-bundle persistent storage volume (`/app/workspace/app-data`) that survives uninstall and reattaches on reinstall |
| **Guest Share** | Token-based, time-limited, revocable link that gives unauthenticated (or authenticated) viewers chat-only access to a specific agent install; protected by a 4-digit security code, scoped to conversation mode, and optionally exposes a read-only environment panel |
| **Handover** | Agent-to-agent task delegation within the platform |
| **Improvement Request** | An `AgentImprovementRequest` row: a consent-gated, one-directional, one-time share of a frozen session snapshot (transcript + tuning-relevant runtime context, including the prompts and MEMORY files that formed the system prompt) from the session owner to the agent's owner — the bundle publisher for a consumer install, or the requester themselves otherwise. Raised via a session's "Improve Agent" menu item or `/session-improve`; reviewed from a Configuration-tab card or `cinna improve` CLI verbs; downloadable as a self-describing ZIP archive. Frozen at consent — nothing reads the source session again — and has no withdrawal in v1. |
| **Workspace** | Isolation boundary for user's agents, sessions, and resources |
| **AI Function** | LLM utility for text generation, classification, extraction with multi-provider cascade fallback |
| **Disclaimer** | An admin-configurable, server-wide Markdown notice shown at login in a non-dismissible modal. Superusers enable/disable it and choose when it appears (`new_users` — once per browser, `every_login` — once per session). Acknowledgement is tracked in versioned browser storage; editing the text or mode increments the version and re-shows the notice to all users. |
| **Access Policy** | The admin-configured front door, stored on the `ServerConfig` singleton: registration mode (`open` / `invite_only`), allowed email patterns, the password and Google sign-in switches, and the default role for new accounts. Resolved by `AccessPolicyService` and never re-derived elsewhere; a public, rate-limited projection drives the login and signup pages. Patterns gate registration only — an existing user is never locked out by an edit. |
| **Superuser Break-Glass** | Superusers keep password login, recovery and reset even when password sign-in is off for everyone else, so a broken Google configuration cannot lock every administrator out of an instance. |
| **Email Confirmation** | A per-account marker (`email_confirmed`) that gates all non-recovery outbound email. New users start unconfirmed and click a link in a confirmation email; Google-OAuth users and superusers are auto-confirmed. Unconfirmed accounts are limited to 5 standalone agents; confirmed accounts are limited to 50; superusers are unlimited. |
| **Invitation** | An administrator's offer of a pre-created, passwordless account (`user_invitation`, one row per account, migration `89878dff89a6`). Created through the `create_account` chokepoint with `AccountOrigin.INVITE` — ungated, so an invite bypasses `allowed_email_patterns` and invite-only mode. Redeemed by setting a password, by signing in with Google, or by completing a password reset; acceptance also confirms the address. The link carries a `purpose="invite"` JWT whose `jti` **is** the row's `token_jti`, so resending rotates it and every earlier link dies. Status is derived, never stored: `accepted` → `revoked` → `expired` → `pending`. |
| **AI Provider** | An `AIProvider` row (table `ai_provider`, surface `/admin/ai-credentials#providers`, API `/admin/ai-providers`): **a source of AI keys plus the rule for who automatically gets one.** Server-scoped and superuser-only; its secret is never returned by any endpoint. Two **kinds** — `fixed_key` (one pasted model key every member holds a copy of) and `minted` (each person gets their own key, created at the vendor through an organisation administration secret; OpenAI only). Holds `auto_provision_roles` (the rule, read at account creation and never on a later role change) plus the whole wiring policy (`set_as_default`, `set_user_sdk_defaults`, `sdk_default_modes`, `default_model`, `available_models`, the per-mode model overrides, `expiry_notification_date`). Owns exactly one Managed AI credential, written with it in one transaction. **"Provider" never means the vendor** — the vendor is the **type** (`AICredentialType`), which is what an adapter describes and what the `Type` column shows. |
| **Managed AI credential** | A `ManagedAICredential` row: **the key that exists and who holds it.** Owns the member list (`ManagedAICredentialMembership`, one row per person) and reconciles it into per-user `AICredential` children. When it belongs to a Provider (`provider_id`) it is read-only for the key and every wiring flag — writes to those are a `400` naming the Provider, and so is deleting it on its own, because that would leave the Provider auto-provisioning with nothing to grant through. With no Provider it is a **manual** record: its own pasted key, its own wiring policy, its own hand-curated members, and no rule — a manual record never auto-provisions. `provisioning_mode` is derived rather than stored (no Provider or `fixed_key` → `shared`; `minted` → `minted`), and the shadowed policy columns are read through one resolver so a record and its Provider cannot disagree. |
| **Admin-managed AI credential** | An `AICredential` row provisioned by a superuser on behalf of a target user (`is_admin_managed=True`) — the per-user child of a Managed AI credential, which in turn may belong to an AI Provider. Owned by the user and fully participates in existing per-user plumbing; read-only through the user-facing CRUD (owner may use and set as default, but cannot edit/delete/re-key). |
| **Account config** | The native-client credential bundle returned by `GET /external/account-config` — a list of provider descriptors with decrypted API keys, consumed by Cinna Desktop/Mobile to auto-create local LLM providers on login. |
| **`current_user` record** | A synthetic, reserved entry appended to every agent environment's `credentials.json` (`id="current_user"`, `type="current_user"`). Built host-side from the install owner's `User` row — carries `username`, `full_name`, `email`, `email_confirmed`, a `custom_details` map of user-authored `UPPER_SNAKE` key/value pairs, and four locale/communication preference fields: `timezone` (IANA zone, nullable), `language` (nullable), `locale` (BCP-47 formatting locale, nullable), and `conversation_style` (`ai_default` / `concise_direct` / `friendly_chatty`, always present). Not a real `Credential` row; never redacted; unredacted in `credentials/README.md`. Scripts access it via the standard dict-comprehension: `creds["current_user"]`. |
| **User's Details** | The free-text `KEY = value` content a user types in **Settings → My profile → User's Details**. Parsed/normalized into the `custom_details` block inside the `current_user` credentials.json record. Stored as `details_raw` (verbatim, for editor re-open) and `details_parsed` (`{UPPER_SNAKE: "value"}` map) on the `User` row. Editing triggers a fan-out re-sync to all running environments of all agents the user owns via `event_user_details_updated`. The same fan-out is triggered when the user updates any locale or communication preference (`timezone`, `language`, `locale`, `conversation_style`) from the profile settings, since those fields also ride the `current_user` block. |
| **Environment Critical State** | A persisted flag (`critical_state=true`) on an `AgentEnvironment` that coexists with `status="running"`: the Docker container is up, but a post-start provisioning step (package install, credential sync) failed. Sessions, chat, terminals, and logs remain available; CRON-scheduled runs and manual "Run now" are blocked until the owner resolves the issue and rebuilds. Distinct from `status="error"` (container not running). |
| **Agent Env Action Log** | Append-only table (`agent_env_action_log`) recording every significant environment operation — rebuild, setup, package install, credential sync, cron-skip — with full, untruncated error detail. The data source for the env-card "Show details" modal. Rows are written by the lifecycle and the scheduler; never updated; deleted only via CASCADE. |
| **Building Mode** | Agent environment state for configuration and development |
| **Conversation Mode** | Agent environment state for executing tasks and chat |
| **Agent User** | Default role for new signups — can install, chat, and manage settings; cannot create agents or publish bundles |
| **Agent Developer** | Admin-promoted role — unlocks agent creation, building mode, publishing, and all developer UI |
| **A2A** | Agent-to-Agent protocol for cross-platform agent communication |
| **MCP** | Model Context Protocol - exposes agents as tool servers to external LLM clients |
| **App MCP Server** | Universal MCP endpoint that routes messages to agents by AI classification. As of Phase 5 of the channels & identity unification, a singleton `ServerChannel` (`channel_type="app_mcp"`, the platform's `authenticated` transport) — admin kill switch, `visibility` + grants, per-user agent scope, all resolved by `ChannelPolicyService` like every other channel. Routes over exactly what a caller owns (`ChannelCandidateProvider`) plus their enabled identities (`IdentityCandidateProvider`, opt-in, off by default); no route, assignment, or per-agent toggle exists any more |
| **Router Trigger Prompt** | Short natural-language sentence on an agent (and snapshotted into bundle revisions), one of the two fields (with `example_prompts`) every routing consumer — App MCP, Server Channels, identity Stage 2 — classifies against. Bundle install and apply-update **do** propagate it — `InstallService` copies `revision.router_trigger_prompt` onto the installed agent row in both paths. What Phase 5 of the channels & identity unification removed is propagation into a *route*: there is no `AppAgentRoute` left to auto-populate, so the copied prompt is now itself what makes the install routable. Editable by the owner via the focused `PATCH /agents/{id}/router-trigger-prompt` (no developer gate) or the generic agent update |
| **Identity Routing** | Person-level routing candidate available to **every** surface, not just App MCP — users expose themselves as a named identity that other users can address by name, resolved by two-stage routing (Stage 1 picks the person, Stage 2 picks one of their agents; `match_method` is `only_one` or `ai`). `IdentityCandidateProvider` supplies the candidates and each surface composes them into its own ballot: App MCP Stage 1, and (since Phase 3 of the channels & identity unification) Server Channels Pass 1, gated there on the sender's own `allow_identity_routing` opt-in. The session opens in the identity owner's workspace either way. |
| **Identity Agent Binding** | Configuration linking one of a user's agents to their identity, with a trigger prompt and per-caller access control |
| **Identity Binding Assignment** | Per-caller access record granting a specific user access to a specific identity agent binding. Its two booleans point in **opposite directions** and are routinely confused: `is_active` is the identity *owner's* control over who may reach them, while `is_enabled` is the *caller's* own per-person opt-out of reaching that owner (the row is keyed by `target_user_id`, and `toggle_identity_contact` filters on it). One person-level `is_enabled` toggle governs every surface — App MCP and Server Channels alike — deliberately not duplicated per channel |
| **MCP Connector** | Configuration that connects an agent environment to an external MCP server |
| **Agent Webapp** | Lightweight data dashboard (HTML/CSS/JS) served from an agent's workspace via shareable URLs |
| **Webapp Share** | Token-based public access to an agent's webapp for unauthenticated viewers |
| **Agent REST API** | A capability-narrowed HTTP API that a producer agent builds from plain Python functions decorated with the `cinna_api` SDK and exposes through the platform proxy. The powerful upstream credential stays in the producer container; consumers get only the surface the producer chose to expose. A deterministic, no-LLM-at-call-time code-to-code channel (contrast with A2A and MCP). |
| **`agent_api` credential** | A `CredentialType.AGENT_API` credential storing `{ base_url, token, spec_url, label, producer_agent_id }` — it **is** the connection between a consumer agent and a producer's REST API. Created by the "Connect Agent API" helper, it rides the existing credential sync / whitelist / redaction pipeline; `token` is redacted in agent prompts. Deleting it disconnects (cascade-deletes the token). Cross-user sharing is safe because the shared value is the narrowed proxy, not the upstream secret. |
| **`agent_api_token`** | Opaque scoped token (SHA256 hash at rest, 8-char display prefix) that authenticates consumer calls to a producer agent's REST API. Internal machine credential — never created or revoked by hand: it is minted by the connect helper and bound to its `agent_api` credential via `credential_id` (`ON DELETE CASCADE`), so deleting the credential is the only way to revoke. Never expires. `read_only_override` may only narrow the producer's `policy.yaml`, never widen it. |
| **`owner_identity_token` (agent_api caller identity)** | A synthetic, host-computed `credentials.json` entry (`id="owner_identity"`, `type="owner_identity_token"`) injected into a consumer env that has ≥1 `agent_api` connection — a sibling of the `current_user` record. It carries a narrow, audience-restricted (`aud="agent_api_caller"`) JWT the agent sends on Agent REST API calls; the proxy verifies it, resolves the *install owner*, **strips** it, and injects trusted `X-Cinna-Caller-*` headers so the producer learns who is calling. Anonymous-by-default (absent/expired ⇒ no headers, never an error); never reaches the producer. Not a real `Credential`; never redacted; re-minted on every credential sync. |
| **Agent REST API access grant** | An `agent_api_access_grant` row, one per `(producer_agent, user)`, by which a producer's owner assigns **capability scopes** to individual platform users from the **Access & Scopes** card (gated by the `agent_api_identity_enabled` opt-in flag). The proxy resolves the grant **live** on every call and injects `X-Cinna-Caller-Scopes` (space-separated); editing scopes takes effect on the next call. The producer is the primary enforcer (`caller.scopes` in the `cinna_api` SDK); the platform optionally hard-denies scope-gated endpoints declared in `policy.yaml` at the edge. |
| **Agent REST API — connection vs. key** | The two products behind one `agent_api` proxy, discriminated by `AgentApiToken.kind`. A **connection** (`kind="connection"`) wires one platform agent to another: anonymous by default, machine-only token, never shown, synced into the consumer's `credentials.json`. A **key** (`kind="external"`, see below) hands a human-operated caller outside the platform the same access. Both ride the identical proxy, `policy.yaml`, and live scope-grant machinery, so a producer's authorization code (`caller.user_id`, `caller.has_scope(...)`) is the same for either caller — one authorization path, not two. |
| **`agent_api` external key** | A hand-issued `agent_api_token` (`kind="external"`) that lets a caller outside the platform — a laptop script, server, or cron job — call a producer agent's REST API as if it were a peer agent. Bound at mint time to a `subject_user_id` (immutable — reassigning identity is revoke + re-issue, never an edit) and optionally an expiry; minting is gated by the producer's `agent_api_external_access_enabled` opt-in, which also acts as a live kill switch on every call. Revealable (once, then again on demand) only via `POST /credentials/{id}/agent-api-key/reveal`, which audits every reveal (`AGENT_API_EXTERNAL_KEY_REVEALED`); the generic `with-data` credential-detail endpoint strips the token for a key instead of showing it. Never synced into `credentials.json`, never shareable (`allow_sharing` forced off), and categorized under **My Credentials** rather than Automatic. Scopes live on the same `agent_api_access_grant` row a connection's caller would use — the key answers *who*, the grant answers *what*. |
| **Agent-to-Agent MCP Connector** | A connector flagged `is_agent_to_agent=true` on `mcp_connector` that a producer agent owner creates to expose their agent as a peer MCP server for other *agents* (rather than external LLM clients). Managed from the Integrations → MCP Connectors card's dedicated sub-tab. Reuses the full MCP connector infrastructure (token verifier, server registry, direct tokens) unchanged; the flag drives producer sub-tab grouping and the consumer picker discoverability. Requires `agent-developer` role to create. |
| **`mcp_provider` credential** | A `CredentialType.MCP_PROVIDER` credential that **is** the connection between a consumer agent's SDK and a remote MCP server — either another platform agent's agent-to-agent connector (`auth_mode="agent2agent"`) or an arbitrary external MCP server (`fixed_token` / `oauth_dcr` / `none`). Created by the "Connect MCP Provider" helper; rides the credential sync / whitelist / redaction / sharing pipeline. Unlike every other credential type, it is **never written to `credentials.json`**: the token is injected into the SDK runtime config (OpenCode `"mcp"` block / Claude Code `options.mcp_servers`) via a per-mode MCP manifest (`user_mcp.json`). Deleting it disconnects and cascade-deletes the bound direct token (agent2agent only). |
| **Account CLI Token** | A `CLIToken` row with `token_type="cli-account"` and `agent_id=NULL`. Created by exchanging an account setup token. Scoped only to the `/account/*` route group — cannot authenticate per-agent sync, exec, workspace, or building-context routes. Carries a 7-day rolling expiry. Stored in `.cinna/account.json` on the developer's machine. |
| **Child Token (CLI)** | A standard `token_type="cli"` `CLIToken` minted programmatically by `POST /account/agents/{id}/mint`. Carries `minted_by_account_token_id` as provenance. Authenticates the unchanged per-agent sync/exec endpoints; revoked individually or by cascade when its parent account token is revoked. |
| **System Notification** | A platform-generated alert about a backend event (e.g. a session error), dispatched to the event owner via email. Distinct from agent-authored messages and the in-app activity feed. Governed by the Notification Catalog and per-user preferences. |
| **Notification Catalog** | The code-only registry (in `notification_catalog.py`) of every system notification type the platform supports. Each entry defines user-facing copy, the default opt-in state, the email template filename, the subject builder, and the dedup key. Adding a new type requires only a new enum value, a catalog entry, and a built email template — no schema migration. |
| **App Sync** | Zero-knowledge server-side sync substrate for native clients (Cinna Desktop, future Cinna Mobile). Stores E2E-encrypted documents partitioned by `collection`; the server never holds a key that can decrypt them. |
| **App Sync Record** | One row in `app_sync_record` representing a single synced entity (a note, a job, a folder, etc.). Keyed by `(user_id, collection, client_entity_id)`; carries an opaque ciphertext payload and a per-user monotonic `seq` for delta pulls. |
| **User Master Key (UMK)** | A per-account 256-bit random key that is the root of the App Sync key hierarchy. Generated once on the first device; never transmitted or stored in plaintext anywhere — the server holds only wrapped (encrypted) copies in `app_sync_key_envelope`. |
| **Key Envelope** | A row in `app_sync_key_envelope` containing the UMK sealed/wrapped under one unlock method (`device`, `recovery`, or `passphrase`). The server stores and returns the `wrapped_key` verbatim and cannot open it. |
| **Skin** | A registered visual overlay (`SkinDefinition` in `frontend/src/config/themes.ts`) selected independently of light/dark mode, applied via `data-skin="…"` on `<html>`. Pure CSS-variable overlay across three tiers — shadcn semantic tokens, Tailwind palette scales, decorative `--skin-*` tokens — with no component-level opt-in. Client-side only (`localStorage`), no backend model. |
| **Device Pairing** | Mechanism for sharing the UMK with a new device via a hardened commit-then-reveal handshake. The joining device generates an ephemeral keypair and a commitment (`blake2b(pubkey ‖ nonce_J)`) and calls `POST /pairing/start`. A trusted unlocked device discovers the request from `GET /pairing/inbox`, posts its `sealer_nonce` via `POST /pairing/inbox/{id}/sealer-nonce`, and seals the UMK via `POST /pairing/inbox/{id}/complete` after the joiner reveals its nonce and the commitment is verified client-side. A 6-digit SAS (`trunc6(blake2b(pubkey ‖ nonce_J ‖ nonce_S))`) is compared out-of-band. The server is a dumb relay: it stores and forwards opaque strings and never verifies the commitment or decrypts the UMK. |
| **Server Channel** | Admin-configured, server-wide inbound transport instance (`ServerChannel`, e.g. Google Chat, Email) that lets people outside the platform message agents. A `webhook` transport (Google Chat) is reached through an unguessable webhook URL; a `polled` transport (Email, since Phase 4 of the channels & identity unification) has no URL at all and is instead pulled by a scheduler — see `ChannelCapabilities.inbound_mode`. Owns its own transport trust model (non-secret `config` + Fernet-encrypted, write-only `encrypted_secrets`, or — for a transport whose credential lives elsewhere, like email's referenced SMTP server — `needs_outbound_credentials=False`), a fail-closed comma-separated email whitelist, and an `auto_register_users` toggle. Superuser-only CRUD via the Channels tab on `/admin/server-configuration`. |
| **Channel Thread Binding** | A `ChannelThreadBinding` row pinning one external channel thread (`thread_key`) to one platform `(user, agent, session)` triple — the conversation state of Server Channels. Created before the session exists (during auto-install) so parked messages have somewhere to land; `pending_install` → `active`, and `failed` self-heals by deletion + re-routing on the next inbound message. A thread belongs to exactly one person — a different sender posting into a bound thread, or the loser of a binding-creation race, is declined rather than routed. |
| **Channel Visibility & Grant** | Two of `ServerChannel`'s four admin-owned availability defaults. `visibility` is `"public"` (every platform user may use the channel) or `"restricted"` (only users with a `server_channel_user_grant` row); a public channel needs no grant rows and never gets any — their absence there means the question was never asked, not that nobody is allowed. Being granted is access, not enablement; both are resolved together by `ChannelPolicyService`. |
| **Channel User Setting** | A `channel_user_setting` row: one person's optional, per-channel override of `ServerChannel`'s admin defaults (`is_enabled`, `agent_scope`, `pinned_agent_id`, `allow_identity_routing`). Absence of the row means "the channel default applies" — it is created lazily, on the user's first edit in Settings → Channels, never at channel-create or account-create time (auto-registered senders and later-created users would otherwise never get one). Resolved against the channel's defaults by `ChannelPolicyService`, the sole place the inherit rules live; `allow_identity_routing` is the one field that never inherits (`NOT NULL DEFAULT false`, no channel-level default): it is the **sender's own** consent that a message of theirs may be routed into somebody else's workspace, where they can read it — never the receiver's gate. It governs **channel and App MCP** identity routing only: External A2A identity access is authorized by its own mechanism (`ExternalAccessPolicy.require_identity_access` plus the owner's binding assignments) and is **not** closed by this switch. Read on every message since Phase 3 of the channels & identity unification (gating the identity half of the Pass-1 ballot, and re-checked before ingesting on an already-bound identity thread), surfaced in Settings → Channels, and audited on transition via `SERVER_CHANNEL_IDENTITY_ROUTING_CHANGED` (severity `medium`). |
| **Channel Agent Scope** | The `agent_scope` resolved per user per channel — `"all"` (every owned agent is a routing candidate, the default), `"list"` (only the agents on the user's `channel_user_agent` list), or `"none"` (no agent routes until the user opts one in). An out-of-scope owned agent is recorded in the routing trace as a skip (`SKIP_NOT_IN_CHANNEL_SCOPE`) rather than silently absent, and routing Pass 2 (catalog auto-install) does not run unless the resolved scope is `"all"` — a decided product semantic, not `allow_auto_install` re-read. |
| **Pinned Channel Agent** | A user's `channel_user_setting.pinned_agent_id` — "always route my messages on this channel to this one agent of mine." Skips classification entirely, records its own routing-trace `match_method="pinned"`, outranks both the `only_one` short-circuit and Pass 2 auto-install (a pinned channel never auto-installs, even when the pin fails to resolve — the sender's instruction stands whether or not the platform can honour it), and has its ownership re-checked at resolution time since the foreign key only guarantees the agent still exists, never that this user still owns it. |
| **Server Auto-Install List** | The server-wide `ServerAutoInstallBundle` list of catalog bundles eligible for Pass-2 channel routing — classified by router trigger prompt only when none of a sender's own installed agents match. List membership is never an implicit permission grant; `CatalogService.user_can_install` still gates every candidate per user. |
| **Routing Decision / Routing Trace** | A `RoutingDecision` row — the durable, superuser-only record of one LLM-mediated routing choice: every candidate considered including rejected ones with a `skip_reason`, the rendered classifier prompt, raw model response, provider/model attempts, verdict, latency, and (message-text-gated) the sender's own words. Every row also carries the **`origin`** that produced it — `server_channel` / `email` / `app_mcp` / `simulate`, with `identity` reserved and unemitted because identity routing is a *stage* inside another origin's decision — and a **`channel_id`**, which since Phase 6 of the channels & identity unification is populated on every live surface (App MCP is a singleton `ServerChannel`, so it has one too). Captured in memory by `RoutingTrace` at the router chokepoint, persisted once the decision settles. See [Auto Routing Tuning](application/routing_tuning/routing_tuning.md). |
| **Reachability Verdict** | A backend-computed, plain-language sentence (one of **16** codes, each with a matching test) explaining why a routing decision went the way it did and what to change. Split by origin for *wording only*, never for the underlying finding: as of Phase 5 of the channels & identity unification, a channel decision and an App MCP decision are diagnosed by the identical logic — ownership and wording (e.g. an owned agent unreachable because it has neither a router trigger prompt nor example prompts) — differing only in the wording each origin gets. Phase 6 of the channels & identity unification replaced the two-way channel-or-App-MCP split with an origin→profile map of **five** remedy profiles (`routing_reachability_service.py`) — `channel`, `email` (the channel profile under its own name), `app_mcp`, an origin-neutral `generic` default, and a reserved `identity` — so an origin the map does not know degrades to origin-neutral wording rather than silently inheriting App MCP's. The six App-MCP-only route-based codes this entry used to describe (e.g. a standalone agent unreachable because it has no `AppAgentRoute`) are deleted outright, not merged, since the route mechanism they diagnosed no longer exists. The headline output of [Auto Routing Tuning](application/routing_tuning/routing_tuning.md). |
| **Transitional Status** | A status value meaning "a background operation currently owns this row and will clear it when it finishes" — `AgentEnvironment.status` in `creating`/`building`/`rebuilding`/`starting`/`activating`, `Session.interaction_status` in `running`/`pending_stream`, `InputTask.status="in_progress"`, `ChannelTurnDelivery.role="draft"`. Set before a long fire-and-forget operation and normally cleared on completion; a backend restart or crash mid-operation (`asyncio.CancelledError`, a `BaseException` that skips every `except Exception` fallback) can abandon a row in this state forever, invisible to every status-filtered scheduler and blocking the user's own way out (e.g. rebuild is refused while an environment is transitional). Reconciled by the Status Repair Sweep, below. |
| **Status Repair Sweep** | A `BackgroundScheduler` cron (`STATUS_REPAIR_*` settings in `config.py`, 2-minute default interval, single-leader via Postgres advisory lock) that reconciles rows abandoned in a Transitional Status across four domains, in one ordered sweep per tick: environments → sessions → input tasks → channel turn deliveries. Verifies live evidence (container probe, session/relay state) before writing — never repairs on age alone — and relies on `AgentEnvironment.status_changed_at` as a liveness heartbeat, stamped unconditionally by the lifecycle's sole status writers (`_set_status`/`_touch_progress`), to tell a slow-but-alive operation from a dead one. See [status_repair.md](system/status_repair/status_repair.md). |

---

## Domain Map

| Domain | Description |
|--------|-------------|
| [agents](#agents) | Core agent lifecycle - creation, configuration, environments, sessions, chat, file management |
| [tasks](#tasks) | Task submission, refinement, triggers, and scheduling |
| [credentials](#credentials) | Credential management, encryption, AI provider keys |
| [application](#application) | User-facing platform features - authentication, roles, integrations, real-time events, workspaces |
| [knowledge](#knowledge) | Git-based knowledge sources, vector search, RAG |
| [sharing](#sharing) | Guest access, workspace collaboration |
| [agentic_teams](#agentic_teams) | Visual org-chart builder for agent orchestration topology — teams, nodes, connections with AI-generated handover prompts |
| [system](#system) | Cross-domain backend infrastructure — background reconciliation that spans multiple domains rather than owning one |
| [development](#development) | Backend/frontend patterns, AI functions, debugging |
| [infrastructure](#infrastructure) | Deployment-level concerns — nginx reverse proxy, well-known URIs, origin-root routing |

---

## Wiki

| Article | Description |
|---------|-------------|
| [Why Cinna Agents?](wiki/skills_plugins_vs_cinna_agents.md) | Skills & plugins vs. Cinna agents — point-by-point comparison for SMBs evaluating AI automation approaches |
| [Claude Managed Agents vs. Cinna](wiki/claude_managed_agents_vs_cinna_agents.md) | Claude Managed Agents (Anthropic's hosted agent API) vs. Cinna agents — architecture, capabilities, and trade-offs |

---

## Feature Registry

### agents

| Feature | Description | Docs |
|---------|-------------|------|
| agent_environments | Docker container architecture, build layers, workspace isolation, multi-image templates | [business logic](agents/agent_environments/agent_environments.md) \| [tech](agents/agent_environments/agent_environments_tech.md) \| [multi-image](agents/agent_environments/agent_multi_image_environments.md) \| [credential rebuild](agents/agent_environments/affected_environments_rebuild.md) \| [sdk session persistence](agents/agent_environments/sdk_session_persistence.md) |
| model_freshness | Centralized model catalog (SSOT for default LLM selection per engine/provider/tier), per-credential available-model discovery cron (daily, single-leader via Postgres advisory lock), computed model-health signal on environments (`ok` / `retired_override` / `unknown_model` / `unverified`), amber badge with cause-specific CTA on environment cards, and `model_deprecated` system notification on new deprecation transitions. Detect-and-nudge only — the user always triggers model changes. Admin-curated `default_model` on managed credentials takes precedence over the catalog tier default (env override still wins); admin-curated `available_models` is preferred over `discovered_models` in model-picker datalists. Distinct from image-tag `is_stale` (config health, not Docker rebuild). | [business logic](agents/agent_environments/model_freshness.md) \| [tech](agents/agent_environments/model_freshness_tech.md) |
| agent_env_critical_state | First-class persisted critical state for running-but-degraded environments: when a Docker container starts healthy but a later provisioning step fails (custom package install, system package install, credential sync), `status` stays `running` while `critical_state=true` flags the env for owner attention. A persisted append-only `agent_env_action_log` table captures full error detail. Amber "Action required" env-card block with "Show details" modal; owner email notification (`environment_critical` type, fire-once per transition, dedup-throttled); CRON gating (critical envs skip scheduled runs, manual "Run now" returns 400); offline supersedes critical (health scheduler clears both flags together). | [business logic](agents/agent_environments/agent_env_critical_state.md) \| [tech](agents/agent_environments/agent_env_critical_state_tech.md) |
| agent_env_console | Live container log tailing and interactive web terminal (PTY shell) per environment — two consoles in a right-side xterm.js drawer; logs served host-side from the Docker adapter (works immediately); terminal served by env-core `WS /shell/pty` (requires rebuild); owner+developer gate with full SecurityEvent audit | [business logic](agents/agent_environments/agent_env_console.md) \| [tech](agents/agent_environments/agent_env_console_tech.md) |
| agent_prompts | System prompt construction for building and conversation modes; three bidirectional prompt docs (workflow/entrypoint/refiner) use three-way reconcile + LWW tiebreak between DB and env-container; pull-only caches (STATUS.md, CLI_COMMANDS.yaml) unified under the Synced Workspace File Registry; personal per-install memory injection from `app-data/memory/*.md` (20,000-char cap (≈5,000 tokens), no-op when empty, never synced to DB) | [business logic](agents/agent_prompts/agent_prompts.md) \| [tech](agents/agent_prompts/agent_prompts_tech.md) |
| agent_commands | Slash commands in agent sessions — `/files`, `/session-recover`, `/session-reset`, `/rebuild-env`, `/agent-status`, and the `/run:*` family — with autocomplete popup UI. Command output with `include_in_llm_context=True` is forwarded to the next LLM turn via a `<prior_commands>` XML block | [business logic](agents/agent_commands/agent_commands.md) \| [tech](agents/agent_commands/agent_commands_tech.md) \| [files](agents/agent_commands/files_command.md) \| [recovery](agents/agent_commands/session_recovery_command.md) \| [reset](agents/agent_commands/session_reset_command.md) \| [rebuild-env](agents/agent_commands/rebuild_env_command.md) \| [autocomplete](agents/agent_commands/slash_command_autocomplete.md) \| [agent-status](agents/agent_commands/agent_status_command.md) \| [non-llm context bridging tech](agents/agent_commands/non_llm_context_bridging_tech.md) |
| agent_plugins | Resilient, manifest-driven plugin system. Each env has a bind-mounted `/app/workspace/plugins/manifest.json` (SSOT); the container install routine fetches marketplace plugins via `git clone` at the pinned commit and verifies bundle-seeded plugins at every setup (new container, post-rebuild) — no backend file cache. `settings.json` is derived from the manifest, files-present only, so a failed plugin is never handed to the SDK as a missing path. Bundle-sourced plugins (`source=bundle`, `plugin_id=NULL`) ride the bundle revision snapshot; `plugin_sync.merge` preserves consumer toggles on apply-update. Per-plugin `PluginInstallResult` drives non-blocking error surfacing: `PLUGIN_SYNC_WARNING` realtime event (amber banner) + `PLUGIN_SYNC_FAILED` system notification (email). OpenCode: plugin-declared MCP servers registered natively, `commands/*.md` copied into runtime command dir, unsupported capabilities (skills/hooks) reported via `plugin_capability_warning` event. | [business logic](agents/agent_plugins/agent_plugins.md) \| [tech](agents/agent_plugins/agent_plugins_tech.md) |
| agent_schedulers | Multi-schedule CRON execution with natural language input, two schedule types (static prompt, script trigger), and execution logging | [business logic](agents/agent_schedulers/agent_schedulers.md) \| [tech](agents/agent_schedulers/agent_schedulers_tech.md) |
| agent_webhooks | Per-agent authenticated HTTP webhooks — two trigger types (session: starts a new agent session seeded with the payload; script: runs a shell command in the agent's Docker environment), bearer-token auth with Fernet encryption and one-time reveal, immutable invocation logs | [business logic](agents/agent_webhooks/agent_webhooks.md) \| [tech](agents/agent_webhooks/agent_webhooks_tech.md) |
| agent_handover | Agent-to-agent task delegation and inbox creation | [business logic](agents/agent_handover/agent_handover.md) \| [tech](agents/agent_handover/agent_handover_tech.md) |
| agent_environment_core | Server-side core running inside Docker containers: HTTP API, SDK adapters, prompt generation. Two SDK engines: Claude Code (Anthropic; MiniMax temporarily disabled in UI), OpenCode (Anthropic, OpenAI, Google, OpenAI-compatible). Each environment links separate AI credentials per mode (building/conversation) with optional per-mode model overrides. OpenCode uses MCP bridge servers for custom tools. All adapters emit unified lowercase tool names via `tool_name_registry.py`. | [business logic](agents/agent_environment_core/agent_environment_core.md) \| [tech](agents/agent_environment_core/agent_environment_core_tech.md) \| [multi-sdk](agents/agent_environment_core/multi_sdk.md) \| [multi-sdk tech](agents/agent_environment_core/multi_sdk_tech.md) \| [knowledge tool](agents/agent_environment_core/knowledge_tool.md) \| [create agent task tool](agents/agent_environment_core/create_agent_task_tool.md) \| [tools approval](agents/agent_environment_core/tools_approval_management.md) \| [tools approval tech](agents/agent_environment_core/tools_approval_management_tech.md) \| [opencode interactive questions](agents/agent_environment_core/opencode_interactive_questions.md) \| [opencode interactive questions tech](agents/agent_environment_core/opencode_interactive_questions_tech.md) \| [claude code interactive tools](agents/agent_environment_core/claude_code_interactive_tools.md) \| [claude code interactive tools tech](agents/agent_environment_core/claude_code_interactive_tools_tech.md) |
| agent_environment_data_management | Environment data flow — denylist-driven bundle-owned vs. App Data vs. credentials vs. runtime data; install creation, apply-update (with stale-bundle-dir prune), and env-migration copy profiles | [business logic](agents/agent_environment_data_management/agent_environment_data_management.md) \| [tech](agents/agent_environment_data_management/agent_environment_data_management_tech.md) |
| agent_bundles | Desktop-app-style bundle / install model — agents published as versioned bundles, installed by other users, push-updated; full-workspace denylist snapshot (schema_version 2, with `workspace/` subtree; v1 revisions remain readable); fail-loud on missing publisher workspace; bundle ID format, visibility/grants, revisions, publisher working install; the revision's `router_trigger_prompt` is copied onto the installed agent row at install time and refreshed on apply-update (Phase 5 of the channels & identity unification deleted the route the install used to auto-create from it — the prompt itself is still propagated, and is what makes the install routable); publisher schedules are snapshotted into the revision and materialised on consumer installs (consumers can enable/disable and run, but not create/edit/delete); the revision's per-mode SDK block, including publisher-pinned `model_override_*`, is restored onto every new install's environment (outranking the installer's own saved default override), not just git checkout — except a mode whose SDK resolves to `openai_compatible`, where the imported override is dropped as non-portable to the consumer's own endpoint; credential-sharing drift detection (publish-vs-live `provided_by` diff) with republish nudge in the Bundle tab; unified Permissions management card on the Bundle tab unions bundle catalog access with per-user Agent REST API producer scopes in one compact per-user row list — list membership itself is bundle access, and a single per-row Remove cascades bundle-grant plus all manageable-producer-scope revocation — replacing the old inline grants picker, with owner-gated read-only treatment for producers the publisher does not own; automatic-mode installs converge onto a new revision via three cooperating paths — the suspension-time running→suspended hook, a publish-time fast-path sweep, and a periodic fleet-wide sweep (self-healing on revision mismatch, with failure backoff) — so an install whose environment is already suspended/stopped/absent at publish time is no longer stuck forever; per-install update mode is editable anytime from the agent's Config tab (`BundleInstallationCard`, "Update now" stays usable in both modes), independent of the bundle-level `default_install_mode` dropdown which only seeds new installs going forward; **a bundle wired to an admin-managed AI credential degrades to "user provides"** (deliberate — it stops a publisher redistributing an administrator's key past the member list the admin curated) and the installer is told so by the runtime gate's fourth reason `publisher_credential_unshareable`, which has its own copy in the chat reply and the `SetupNeededBanner` because the generic "contact the publisher to restore access" names a fix that does not exist — **except for an installer who already holds a share**, where both the gate and `InstallService._linkable_publisher_ai_credential` look for the `AICredentialShare` *before* asking the policy, keep the credential, and report nothing (the install works; saying it "cannot be shared" would be false), with those pre-existing share rows deliberately left in place as an accepted trade whose named price is that one can stay live indefinitely if its publisher never publishes again; the *publisher* is told the same fact where they can act on it, at publish time, via `PublishService.publisher_ai_credential_notices` → `publish_notices` on the publish response, rendered as a dismissible persistent callout on the Bundle tab (not a toast — it belongs in release notes) with every sentence naming an action: installers add their own key, or an administrator provisions the credential to them directly; `InstallService._linkable_publisher_ai_credential` is also what makes the unreachable case a degradation rather than a `400` that fails the whole install (a **vanished** credential id is never linked — the environment column is an FK, so a dangling id is an `IntegrityError`, not a graceful "no credential" fallback) | [business logic](agents/agent_bundles/agent_bundles.md) \| [tech](agents/agent_bundles/agent_bundles_tech.md) |
| agent_git_versioning | Git-backed version control for agent installs — **checkout** a remote repo into a new install, **connect** an existing agent to a fresh remote (initial export push), **pull** remote updates (App Data, credentials, and plugins preserved), **push** live workspace changes (fast-forward-only, persists a bundle revision as dirty-check baseline), **commit history** (`GET /git/commits`, subdir-scoped, bounded shallow clone), **dirty check** (`GET /git/dirty` — workspace hash + prompt-DB drift + `cinna.agent.json` settings drift, intentionally a separate endpoint), and GitOps push-webhook for automated pulls. **Pull conflict resolution:** a blocked pull (local prompt/setting drift) now returns a structured, recoverable 409 (`code: "local_changes"`) instead of a dead end; `POST /git/pull` accepts an optional `conflict_resolution` of `keep_local` (pull, preserve the drifted fields, install intentionally left dirty on them) or `take_remote` (discard local drift) — omitted body keeps the historical fail-loud 409 the GitOps webhook relies on; `GET /git/status` exposes `blocks_pull`/`pull_blocked` so the UI pre-empts the 409 via a `GitPullConflictDialog` showing what blocks, what gets replaced, and what's untouched; a pre-pull backup `AgentBundleRevision` is captured on both resolutions since the workspace is replaced wholesale either way. First UI: "GIT Versioning" card in the Integrations tab with connect form (deploy-key picker with quick-generate), connected view with update banner ("Review & pull" when dirty), dirty-gated "Commit Agent" button, commit history, and disconnect. Git is an external layer over `AgentBundleRevision`; orthogonal to Mutagen (a folder can be both); two-writer model (backend deploy key + developer own creds, both ff-only, fail-loud conflicts). SSH-key auth host-side only; egress/SSRF guard on every network call; `.gitignore` and snapshot denylist derived from the same constants. `GET /api/v1/cli/git-coordinates` (CLI-token, no deploy key) exposes repo coordinates to the cinna-cli for sparse-checkout local dev | [business logic](agents/agent_git_versioning/agent_git_versioning.md) \| [tech](agents/agent_git_versioning/agent_git_versioning_tech.md) |
| agent_app_data | Per-user, per-bundle persistent App Data volume (`/app/workspace/app-data`) keyed by `(user_id, bundle_id, catalog_type)` — survives uninstall/reinstall, Settings tab management, manual wipe on orphaned volumes, plus a background GC that reclaims on-disk dirs with no DB representation after account/install deletion; four sub-directories: `storage/`, `uploads/`, `cache/`, `memory/` (personal per-install agent memory, injected into system prompts) | [business logic](agents/agent_app_data/agent_app_data.md) \| [tech](agents/agent_app_data/agent_app_data_tech.md) |
| agent_credentials | Credential syncing to agent environments, whitelisting, redaction, OAuth refresh, three sharing modes (user / publisher / template), blast-radius-gated deletion (Tier 0/1/2, HTTP 409 + force escape hatch for PBP credentials in published bundles); `service_uri` slot-id matcher (Tier 0a/0b) enables per-user token auto-detection across differently-named credentials at bundle install time; publish-vs-live `provided_by` drift detection with republish nudge; a synthetic `current_user` entry (owner identity + `custom_details` map) is appended to every environment's `credentials.json` — not a real credential, never redacted, always reflects the install owner; `/credentials` page uses three filter tabs (My / Automatic / Bundle) driven by a server-computed `category` field; `CredentialShare.source` provenance marker (`"direct"` vs `"bundle_install"`) distinguishes direct shares from bundle-install PBP shares | [business logic](agents/agent_credentials/agent_credentials.md) \| [tech](agents/agent_credentials/agent_credentials_tech.md) \| [oauth](agents/agent_credentials/oauth_credentials.md) \| [whitelist](agents/agent_credentials/credentials_whitelist.md) \| [google SA](agents/agent_credentials/google_service_account.md) \| [ssh key](agents/agent_credentials/ssh_key_credentials.md) \| [ssh key tech](agents/agent_credentials/ssh_key_credentials_tech.md) \| [sharing](agents/agent_credentials/credential_sharing.md) \| [sharing tech](agents/agent_credentials/credential_sharing_tech.md) \| [security hardening](agents/agent_credentials/credential_security_hardening.md) \| [security hardening tech](agents/agent_credentials/credential_security_hardening_tech.md) \| [add credential widget](agents/agent_credentials/add_credential_widget.md) |
| agent_file_management | File upload/download, workspace file viewing, storage quota, garbage collection | [business logic](agents/agent_file_management/agent_file_management.md) \| [tech](agents/agent_file_management/agent_file_management_tech.md) \| [remote db viewer](agents/agent_file_management/remote_database_viewer.md) |
| agent_message_attachments | Agents attach workspace files to their reply messages via `<cinna_attach>` tags — backend materialises files into durable storage, injects `attachment` streaming events, renders inline cards in web chat (with preview modal), and delivers native `FilePart(FileWithUri)` objects to A2A clients (Cinna Desktop/Mobile) via signed 1-hour download URLs | [business logic](agents/agent_file_management/agent_message_attachments.md) \| [tech](agents/agent_file_management/agent_message_attachments_tech.md) |
| agent_webapp | Lightweight data dashboards served from agent workspace via shareable URLs, with dynamic Python data endpoints | [business logic](agents/agent_webapp/agent_webapp.md) \| [tech](agents/agent_webapp/agent_webapp_tech.md) \| [chat widget](agents/agent_webapp/webapp_chat.md) \| [chat tech](agents/agent_webapp/webapp_chat_tech.md) \| [chat context](agents/agent_webapp/webapp_chat_context.md) \| [chat context tech](agents/agent_webapp/webapp_chat_context_tech.md) \| [chat actions](agents/agent_webapp/webapp_chat_actions.md) \| [chat actions tech](agents/agent_webapp/webapp_chat_actions_tech.md) \| [actions context](agents/agent_webapp/webapp_actions_context.md) \| [actions context tech](agents/agent_webapp/webapp_actions_context_tech.md) |
| agent_status_tracking | Agent self-reported status via `app-data/storage/STATUS.md` (per-install App Data, not bundle content) — optional YAML frontmatter (severity/summary/timestamp), DB-cached snapshot, `/agent-status` slash command, REST endpoints, agent-card footer in the agents list, post-action event-driven pull, A2A `agent/status` method, and a configurable **status refresh command** (shell or `/run:<name>` CLI reference) that runs inside the container before every forced/live fetch; managed via the Configuration tab **Agent status** card | [business logic](agents/agent_status_tracking/agent_status_tracking.md) \| [tech](agents/agent_status_tracking/agent_status_tracking_tech.md) \| [/agent-status command](agents/agent_commands/agent_status_command.md) | <!-- nocheck -->
| cli_commands | Agents declare named shell commands via `docs/CLI_COMMANDS.yaml` in their workspace. The platform syncs and caches the parsed list on `AgentEnvironment`, exposes commands as dynamic `/run:<name>` entries in the slash-command autocomplete popup (with tooltip showing the resolved shell string) plus a `/run-list` discovery entry (shown when the cache is non-empty), hides the bare `/run` from the popup, and fires `CLI_COMMANDS_UPDATED` events. `/run:<name>` queues a pending message, streams execution via agent-env `POST /command/stream` through `SessionStreamProcessor`'s batch loop, emits `STREAM_COMPLETED`/`STREAM_INTERRUPTED`/`STREAM_ERROR` on finish (wires up `session_interaction_status_changed` WS fanout, activity log, task/CLI/status cache refresh), and persists output as a terminal/markdown system message. CLI commands also surface as `cinna.run.*` A2A skills on the authenticated extended agent card. Command output is forwarded to the next LLM turn via the non-LLM context bridging feature. | [business logic](agents/cli_commands/cli_commands.md) \| [tech](agents/cli_commands/cli_commands_tech.md) | <!-- nocheck -->
| agent_api | Producer agent exposes a capability-narrowed REST API from plain decorated Python functions (`cinna_api` SDK) inside its container. env-core supervises the uvicorn child (lazily spawned on first call, idle-reaped after 5 min), harvests the OpenAPI spec import-only, and caches it on `AgentEnvironment`. Backend enforces declarative `policy.yaml` guardrails (method allowlist / body cap / rate limit / path allowlist) at the proxy edge. Consumers connect via a new `agent_api` credential type that rides the existing credential sync / whitelist / redaction pipeline. Cross-user sharing via `CredentialShare`. One-click "Connect to another agent" helper. `agent_api` connection credentials appear under the **Automatic Credentials** tab in `/credentials` (server-computed `category="automatic"` covers `agent_api` and **agent2agent** `mcp_provider` credentials; external/manual `mcp_provider` servers are "My Credentials"). Bundle publisher-provided `agent_api` credentials supported via the one-shared-token model; pair with a per-user `api_token` + `service_uri` for scoped access. **Caller identity & producer scopes:** a synthetic, auto-injected `owner_identity_token` (sibling of `current_user`) lets the proxy attribute each call to the install owner and inject trusted `X-Cinna-Caller-*` headers (verified-then-stripped; anonymous-by-default); the producer's owner assigns per-user capability scopes (`agent_api_access_grant`, live-resolved, opt-in via `agent_api_identity_enabled`) read in code via the `cinna_api` SDK `caller` accessor, with optional `policy.yaml`-declared edge enforcement. **External Keys:** a producer can also mint a revealable, identity-bound `agent_api` **key** (`AgentApiToken.kind="external"`, own `agent_api_external_access_enabled` opt-in / live kill switch) that lets a laptop script, server, or cron job call in from outside the platform with a bound `subject_user_id` — the same `caller` accessor and scope grants a connection uses, so the producer needs no second authorization path; keys are revealed only via an audited dedicated endpoint, never synced into `credentials.json`, never shareable, and land under **My Credentials** rather than Automatic. A2A is intelligence-to-intelligence; MCP is LLM-to-tool; `agent_api` is **code-to-code** (no LLM at call time). | [business logic](agents/agent_api/agent_api.md) \| [tech](agents/agent_api/agent_api_tech.md) \| [spec viewer](agents/agent_api/spec_viewer.md) \| [spec viewer tech](agents/agent_api/spec_viewer_tech.md) |

### tasks

| Feature | Description | Docs |
|---------|-------------|------|
| input_tasks | Task submission, AI refinement, execution workflow, comments (agent findings/results), file attachments, short-code IDs (TASK-1/HR-42), status history, subtask delegation, team-scoped tasks. The email-originated task flow ("Send Answer" AI reply, `source_email_message_id`/`source_agent_id`) is gone as of Phase 4 of the channels & identity unification — incoming email can no longer create an `InputTask`; see `email_integration` below. | [business logic](application/input_tasks/input_tasks.md) \| [tech](application/input_tasks/input_tasks_tech.md) |
| task_triggers | Automated triggers - CRON schedules, webhooks, date-based | [business logic](application/input_tasks/task_triggers.md) \| [tech](application/input_tasks/task_triggers_tech.md) |
| tools_approval | Agent tool execution approval management | [business logic](agents/agent_environment_core/tools_approval_management.md) \| [tech](agents/agent_environment_core/tools_approval_management_tech.md) |

### credentials

| Feature | Description | Docs |
|---------|-------------|------|
| ai_credentials | LLM provider API keys, named credentials, prioritized default resolution, environment linking, sharing. Supported types (UI): Anthropic, OpenAI, OpenAI-compatible, Google. (MiniMax is temporarily disabled in the UI — not currently supported; backend code remains.) **The table's invariant is that every `AICredential` row that exists is usable** — no placeholder row, no empty key, no "preparing" credential — which is what lets the discovery cron, the desktop account-config bundle and the environment credential bag all read it unfiltered and be correct to. Per-user key minting keeps its pre-key state on a membership row instead (see `admin_ai_credential_provisioning`). **No child of an admin-managed record can be shared, in either provisioning mode** — `share_credential` refuses on `is_shareable(credential)`, which is the plain `is_admin_managed` column and so refuses an orphaned child (parent force-deleted, FK `SET NULL`) too, raising the typed `AICredentialNotShareableError` rather than a bare `400` so its one caller can tell a permanent policy refusal from a transient failure. Widened from minted-only in the fourth pass: a `shared`-mode child is deleted by reconcile on member removal, so sharees lose access just as silently — the same failure with a different cause of death. The onboarding answer the dashboard wall reads (`api_key_onboarding_state`) is now **provider-agnostic on both halves** — `has_key` is `owner_ids_with_a_default`, any provider — because an environment can run on a non-Anthropic credential alone (the gate is per-SDK, not per-provider) and the old Anthropic-scoped `has_key` made `preparing → has_key` unreachable, walling every member whose mint had just succeeded. "Has a credential" is not "has a default": `set_as_default` defaults off, and the admin's invite success message now derives from the same predicate the user's wall does. `ids_from_openai_shape` restored its raise for a payload that is neither dict nor list — an empty success overwrote the model cache **and** cleared the discovery error, flipping health from UNVERIFIED to OK; only a bare array and unusable *entries* inside a good envelope are tolerated. Zero-touch onboarding phase 5 also introduced `backend/app/services/ai_providers/` — one adapter module per provider behind one registry, which absorbed the per-provider probe listers, credential-bag slot tables, SDK-engine maps and the `sk-ant-oat` classification that were previously restated across six modules. Its property (*no dict keyed on `AICredentialType` members outside the package*) is enforced by an architecture test whose allowlist is **empty and meant to stay empty**; two string-keyed tables in `environment_lifecycle._write_opencode_config` are deliberately outside its reach. Known: a rejected Google key bypasses the `invalid_key` mapping because `google-genai` raises its own `ClientError` — symptoms are a **500 from Test Connection** and **`"ClientError"` in the discovery cron** (pre-existing, preserved deliberately). | [business logic](application/ai_credentials/ai_credentials.md) \| [tech](application/ai_credentials/ai_credentials_tech.md) \| [provider adapters tech](application/ai_credentials/provider_adapters_tech.md) \| [anthropic types](application/ai_credentials/anthropic_credential_types.md) \| [affected envs](application/ai_credentials/affected_environments_widget.md) \| [ai functions routing](application/ai_credentials/ai_functions_sdk_routing.md) |
| admin_ai_credential_provisioning | Superuser creates a **Managed AI Credential** parent record (canonical encrypted key + target-user set + default flags + optional curated model list) that is reconciled into per-user child `AICredential` rows. Reconcile diffs desired vs. actual membership: Add creates children, Remove deletes them (Tier-2 blast-radius gated), Update writes changed fields/key/curated-model metadata through to existing members — idempotent. Children are read-only for their owners but participate in all existing per-user plumbing. Admin UI ships as **Admin → AI Credentials** (`/admin/ai-credentials`; renamed from "LLM Providers" / `/admin/llm-providers` in zero-touch-onboarding phase 4 — **UI only**, the backend prefix, OpenAPI tag and `AdminLlmProvidersService` are unchanged, and the old route is a redirect stub): fleet-wide table with filter-by-user, unified create+edit dialog (`ManagedCredentialDialog`, `UserAllowlistPicker` for membership, **"Fill top 10 models"** helper that auto-runs Test Connection and fills the top 10 discovered models plus auto-sets a provider-appropriate default, reconcile-result toasts), and per-record edit/set-default-all/delete actions. Also includes a native-token-gated `GET /external/account-config` endpoint (backend-only) that will let Cinna Desktop/Mobile auto-create local LLM providers on login. **Zero-touch onboarding phase 2** adds auto-provisioning: `auto_provision_roles` on the parent record grants the credential to every *newly created* account holding one of those roles, via `AccountProvisioningService.on_account_created` at the single account-creation chokepoint — never on a later role change (an explicit **Apply to existing users** action, with a `?dry_run=` preview quoting both the candidate count and how many chosen defaults it would replace, covers accounts that already exist). Provisioning failures never fail account creation; they become a skip plus a medium-severity event in the new owner's feed. Per-mode `model_override_conversation` / `model_override_building` pin a model on each member's profile with a three-valued update contract (omitted = unchanged, `""` = clear and retract members' pins that still match, a model id = set and write through). Only one auto-provisioning record may own a `(role, mode)` default slot — a second is refused `409 auto_provision_conflict`, validated on the slots a request *newly* claims so a rename can never fail for a conflict it did not introduce. UI: an **Auto** role-chip column and the Apply action on `/admin/ai-credentials`, and per-credential **User · Dev · Admin** role toggles in its own half-width **Company AI credentials** card on **Server Configuration → Access** (capped at 5 rows with a link to the full list). **Phase 3** adds a second entry point over the same body, `AccountProvisioningService.provision_explicit(…, managed_credential_ids, actor)` — the invite wizard's explicit list, sharing `_provision` and the outer never-fail net with the automatic path so an invited and a self-registered account of the same role cannot diverge; `None` means "grant what this role would have got anyway", `[]` means the admin unticked everything, a ticked id that no longer exists is reported as a `managed_credential_not_found` skip, and `actor` is required so the architecture test's single sanctioned `actor=None` site stays inside this service. The invite wizard's *provisioning step* still has no key-entry control (the account does not exist at that step); **phase 5 put the affordance on the invite success screen instead** — a link ("Add an AI key for `<email>`") to `/admin/ai-credentials?newCredentialFor=…` that opens the ordinary `ManagedCredentialDialog` controlled and pre-seeded with the new account as its only member, hidden entirely for an inactive account. **Phase 5** adds **per-user key minting**. A parent record now has a `provisioning_mode`: `shared` (the historical behaviour — one pasted key, copied to every member) or `minted` (each member gets their own key, created at the provider through a `ProviderAdminCredential` — an instance-wide administration secret in its **own table**, because `model_discovery_service` probes every `ai_credential` row and `/external/account-config` ships every owned one to the desktop). **Membership became a row** (`ManagedAICredentialMembership`, one per (parent, user), migration `ed8d6a23f13c` with a loud-failing backfill): the previous *derivation* from the children is gone, not parallel, and the row carries an explicit status — `not_applicable` / `pending` / `minting` / `provisioned` / `failed` / `suspended`, never inferred from a NULL. A `failed` row is durable and is never deleted to tidy up, which is what makes "account creation never fails because provisioning failed" honest rather than merely true; the way out is an explicit per-member **Retry** route. No provider call ever happens on a request path — a converge scheduler (1 min, single-leader via the new `app.core.db.leader_session`) mints against the recorded intent, claiming the row *before* any configuration check, bounded at 5 attempts with a lengthening backoff, and committing the provider handles **before** the key so a crash costs a wasted key rather than a leaked one. Deactivation revokes a minted key and suspends the membership, reactivation mints a fresh one, deletion snapshots the handles before the cascade and revokes after it commits — the deactivation/reactivation hooks are wired at **two** `is_active` sites (`update_user`'s transition check and `InvitationService._apply_reinvite_updates`, the one a reader would not think to look for), and **deletion is explicitly not a third**: it calls `on_account_deleted` instead, because a deleted account's rows are gone by the time anything could read them; shared credentials are untouched by all of it, and every removal is **delete-first, revoke-second**. **What may be claimed about a minted key is bounded**: it carries write access to the project's API resources, and the *only* enforcement lever is the project's monthly spend limit, verified `enforcement.status == "enforcing"` at **setup, before any key exists** — a limit that reports `inactive` is not a cap and minting is refused. Key `scopes` are deliberately not requested (the vocabulary is not authoritative in any artifact this repo holds; `scopes_requested`/`scopes_granted` are recorded so hardening is a change, not a rewrite) and `model_permissions` is a known, deliberately-unpulled lever — so nothing here may be described as narrowing a key's permissions. Single-project only; project-per-user was dropped because projects cannot be deleted at the provider. **Anthropic cannot mint keys — verified**, so for it (and every provider but OpenAI) a key is *pasted*, which is the normal path and not a fallback. Owner-facing: `GET /ai-credentials/provisioning` (a separate list, because the credentials list holds only usable credentials) and `api_key_onboarding_state` on the user status route — a three-valued server answer (`has_key`/`preparing`/`needs_key`) replacing the boolean the dashboard's paste-a-key wall used to read, so nobody is walled while a key is being minted for them. Admin UI: `/admin/ai-credentials` gains a **Provider keys** tab and a **Keys** column with per-member status and Retry. **Fourth pass (whole-feature seam review) corrected five things at the seams.** (1) The sharing refusal widened to every admin-managed child, so a bundle wired to a `shared`-mode child now degrades to "user provides" — a *deliberate* behaviour change that also stops a publisher redistributing an admin's key past the member list the admin curated; the installer sees the new `publisher_credential_unshareable` gate reason, and `GateMissingReason` moved into `app/models/bundles/catalog.py` because two diverged copies had become a 500. (2) `InstallService._linkable_publisher_ai_credential` made the *documented* install fallback real — the publisher credential id had been linked regardless of the refused share, so the env resolver rejected it and the whole install failed with a 400 naming neither the bundle nor the reason (the fourth "documented tolerance that was never true" in this phase). (3) `ManagedReconcileBlock.message` — the server now states the blocked reason sentence, required and drawn from one table beside the model, rendered verbatim by all three consumers; previously each substituted "in use by a published bundle", so an admin blocked by an in-flight mint was told it was a bundle conflict, whose remedy `force=true` stranded a live key. (4) `AIKeyOnboardingState` became provider-agnostic (`owner_ids_with_a_default`) — an environment can run on a non-Anthropic credential alone, so the old Anthropic-scoped `has_key` made `preparing → has_key` unreachable; the dashboard wall no longer full-page-replaces after load (it discarded the user's draft on a 10s poll — the full-page form is latched to first render, `preparing` explains itself in an inline banner and `needs_key` polls), and the admin's invite success message now reads the same predicate as the user's wall. (5) A claim token on the mint state machine: every post-**claim** write is conditional on the row still being claimed — the success path *and* every failure path, because `_record_failure` writes `pending` and an unconditional version would hand a row back to the converge queue that a deactivation had since settled `suspended` — a lost claim revokes the key just minted rather than storing it, and a failed revoke emits the durable event carrying the external ref — *a minted key must never end up live-but-unrecorded*; three races (deactivation, deletion, force-delete of the parent) are covered by tests. Plus `holds_provider_key()` as the single "does this member hold a key" predicate for usage-counting, deletion **and** deactivation, which had been skipping `failed` memberships on the false premise that a terminal failure holds no key. **Fifth pass (review of the fourth pass) corrected the seams it left.** The `AICredentialShare` lookup now runs **before** `is_shareable` in both the readiness gate and `_linkable_publisher_ai_credential`: three facts had been collapsed into one false sentence — *the share exists*, *it still works*, *it will not be created again* — and only the third is about policy, so an installer whose agent was running was being told their publisher's credential "cannot be shared"; a share that exists now yields **no** gate item at all. Leaving those pre-existing share rows alone is an **accepted** trade recorded as accepted, not an oversight: revoking or migrating would break installs that work today, and the named price is that a share stays live indefinitely if its publisher never opens the publish flow again. Fact three is instead told to the **publisher**, at the moment they can act on it — `PublishService.publisher_ai_credential_notices` → the required `publish_notices` on the publish response, rendered as a **dismissible persistent callout** on the Bundle tab rather than a toast (it belongs in release notes), with every sentence naming an action: installers add their own key, **or ask an administrator to provision the credential to them directly**, which keeps the admin's member list authoritative. `UserPublicWithAICredentials.api_key_onboarding_state` became **required with no default** — a default makes the field optional in the generated client, which forces every browser reader to write `?? "needs_key"`, and that fallback *is* the client-side policy the enum's own docstring forbids; it also flattens pending/paused/errored into a confident `needs_key`, and this was the one field a **full-page wall** depended on. `_linkable_publisher_ai_credential` returns `None` for a vanished row: the old comment claimed a dangling id let "the env-side resolver produce the existing 'no credential' path", but the environment column is an **FK**, so that was an `IntegrityError` and a failed install. And `ai_credentials_service.has_a_default` was deleted (no callers; its shape is the one that reintroduces the N+1). **Escalated, deliberately unfixed** — [Known Gap 14](application/ai_credentials/admin_ai_credential_provisioning.md#known-gaps): an unshareable publisher credential with *no* existing share lets the environment fall back to the installer's own default (so the install runs) while the gate still returns `publisher_broken` and blocks **every inbound message**, unclearably by the installer (the scan reads `bundle.publisher_ai_credential_*_id`, which no installer action changes) and with copy pointing at the agent's Credentials tab, which has no AI-credential control — the fallback resolves from Settings → AI Credentials. Awaiting a product decision on whether it should block at all. **ai-credential-providers split the credential from its factory.** One record used to be both a key and the rule that handed it out; it is now an **AI Provider** (`ai_provider`, renamed and extended from `provider_admin_credential` by migration `c23d6b59a8f5`, with `45938a69aee7` adding `claim_held_default_slots` to the membership row) that owns exactly one **Managed AI credential**, while a manual managed credential keeps every control it has and simply has no Provider. The credential's same-named policy columns are **shadowed rather than mirrored** — a second copy of a number the Provider owns goes stale unobserved — and `provisioning_policy.resolve_policy` is the only legal read path, held by two architecture tests, one of which states in its own docstring that it under-approximates (an AST scan sees statically visible receivers, so green means "no *statically visible* second reader"). `/admin/ai-providers` replaces `/admin/provider-admin-credentials`; `GET /admin/ai-providers/adapters` replaces `/admin/provider-adapters` unchanged in shape, and its `can_mint_now` field is **deleted** — under the new model the Provider *is* the organisation, so the conjunction answered `false` for the first OpenAI Provider an admin ever connects, which is the case the create wizard exists to serve; `supports_minting` alone is the term, and a test compares the whole projection before and after two Providers are connected so no field on it can become a function of what is connected. `POST /admin/llm-providers/{id}/apply-to-existing` is **removed** (identical work through a second door for a provider-owned record, and always `candidate_count: 0` for a manual one); `/admin/llm-providers` keeps its path and creates manual records only, with `extra="forbid"` so the three retired fields are a `422` naming the field rather than a `200` that changes nothing. Two conflict rules at two times: **provider vs provider** at write time (`409`, scoped to the slots a request newly claims, so a rename never fails for a collision the admin did not introduce), and **the incumbent wins** at run time — automatic provisioning never takes a default slot somebody holds, grants the credential anyway, and the declined slot is disclosed on the invite response beside the successful grant rather than reported as a failure, while "apply to existing users" and "set default for all" deliberately do overwrite (for a minted Provider that intent is persisted on the membership row, because the grant and the wiring happen on different requests). Two orphan shapes are now named behaviour an admin can hit: deleting a provider-owned credential on its own is a `400` naming the Provider — it would otherwise leave the Provider auto-provisioning with nothing to grant through, and the scan drops such a Provider **silently**, so every later signup for those roles gets nothing while the admin surface shows the rule as active — and the **forced** Provider delete produced the same orphan from inside its own gate until its refusal was keyed on whether the credential row survived rather than on the blocked-member report. The forced delete revokes at the vendor **first**, awaited, because the thing being deleted is the secret the revoke needs. The invite wizard submits `provider_ids` (tri-state unchanged: `null` = the role's automatic set, `[]` = the admin unticked everything, a list = exactly those) and `managed_credential_not_found` gave way to `provider_not_found`. **`set_as_default` off is the "extra key" configuration** — everyone matching the roles receives the key in addition to what they already have, and it displaces nobody's default, which is what lets an Anthropic key for developers coexist with per-user OpenAI keys for everyone. Vocabulary, enforced in code and copy: **Provider** is the entity, the vendor is the **type**, and an adapter describes a type — which is why the AI Credentials page's tabs are now the hash-addressable `#managed-credentials` and `#providers` (the **Provider keys** tab is gone with the route behind it), the managed table's columns are `Type` / `Default key` / `Source`, and the Access tab's card is **Company AI providers**. Earlier phase notes in this row describe the model that preceded the split; this paragraph is the current one. | [business logic](application/ai_credentials/admin_ai_credential_provisioning.md) \| [tech](application/ai_credentials/admin_ai_credential_provisioning_tech.md) \| [provider adapters tech](application/ai_credentials/provider_adapters_tech.md) |

### application

| Feature | Description | Docs |
|---------|-------------|------|
| agent_management | Agent definition lifecycle — identity, prompts, SDK, credentials, integrations, sharing — the config entry point for all platform features | [business logic](application/agent_management/agent_management.md) \| [creation wizard](application/agent_management/new_agent_wizard.md) |
| agent_sessions | Persistent chat sessions between users/external systems and agent environments — lifecycle, modes, streaming, integration types, UI | [business logic](application/agent_sessions/agent_sessions.md) \| [tech](application/agent_sessions/agent_sessions_tech.md) \| [env panel widget](application/agent_sessions/app_env_panel_widget.md) \| [channel ingestion](application/agent_sessions/channel_ingestion.md) \| [channel ingestion tech](application/agent_sessions/channel_ingestion_tech.md) |
| agent_improvement_requests | Consent-gated, one-directional share of a frozen session snapshot (transcript + runtime context) from a session owner to the agent's owner — the bundle publisher for a consumer install, or the requester themselves otherwise. Raised via a session's "Improve Agent" menu item (consent modal) or the sync, non-LLM-context `/session-improve` command (unavailable on guest/webapp-share sessions); reviewed from an owner-only Configuration-tab card with a `new`/`in_progress`/`completed`/`declined` status workflow, or via `cinna improve list\|show\|download\|status` in the account CLI. Snapshot capture caps messages at 50k chars each and the whole transcript at 2 MB (oldest-first drop), keeps a per-message `tool_digest` newest-first, and never captures file bytes, container logs, or (post-scrub) credential values. The runtime context also freezes the install's **prompt documents** — with divergence computed against the installed bundle revision, so a publisher can see that a consumer edited the text they published — and, unless the requester opts out, its **personal MEMORY files** (`app-data/memory`), which are read from the container once at consent time and never wake a stopped environment. Frozen at consent — nothing reads the source session again, and there is no withdrawal in v1. Downloadable as a self-describing, in-memory-built ZIP archive; cross-user downloads are audited | [business logic](application/agent_improvement_requests/agent_improvement_requests.md) \| [tech](application/agent_improvement_requests/agent_improvement_requests_tech.md) |
| auth | User authentication - JWT tokens, password login, Google OAuth, password recovery, and the database-backed access policy that gates registration and sign-in methods (invite-only mode, allowed email patterns, password-auth on/off with a superuser break-glass, non-enumerating reason-code refusals). Since zero-touch onboarding phase 2 every `User` row is built in exactly one place — `UserService.create_account(origin: AccountOrigin)`, pinned by an architecture test — which owns address normalisation, the `is_superuser ⇒ admin` invariant (create is stricter than `update_user`), the policy-derived default role, and auto-provisioning of company AI credentials. `AccountOrigin` (`signup`/`google`/`invite`/`admin`/`external`/`seed`) replaced the phase-1 `ORIGIN_*` string constants and decides whether the registration gate applies at all. **Phase 3 adds invitations**: a superuser pre-creates a passwordless account (`origin=invite`) and mails a link; the `user_invitation` row (migration `89878dff89a6`) holds one live token `jti` that resend rotates, so revocation and single use actually mean something. Five superuser lifecycle routes on `/users/*` (invite, read, resend, revoke, read-link-without-rotating) plus two anonymous rate-limited ones (`POST /invitations/lookup` with the token in the **body**, `POST /invitations/accept` returning the same `LoginResponse` as login). One server-side predicate — `password_accepted` — decides whether the invitee may set a password, and the accept page renders on that boolean alone; `auth_hint` is presentational. Acceptance by password, by Google (auto-link) or by password reset, the last two through a non-raising hook that can never cost someone an authentication they already completed. Security fixes shipped alongside: `POST /password-recovery/{email}` no longer 404s for an unknown address (one generic 200, every refusal a silent no-op), `verify_password_reset_token` now **rejects** any token carrying a `purpose` claim (an invite link used to be a valid reset token, defeating revoke and `jti` rotation), password recovery and reset refuse a never-claimed account whose invitation is not pending, and both failed-login branches burn a dummy bcrypt hash. Recorded residual: the synchronous send still makes a first probe of a registered address measurably slower | [business logic](application/auth/auth.md) \| [tech](application/auth/auth_tech.md) \| [google oauth](application/auth/google_oauth.md) \| [access policy](application/server_configuration/access_policy.md) |
| email_confirmation | Per-account email-confirmed marker that gates all non-recovery outbound email; token-based confirm flow; 300s cooldowns on confirmation and password-recovery resends; tiered agent-creation limits (unconfirmed = 5, confirmed = 50, superuser = unlimited); Google-OAuth and superuser auto-confirm; publisher-email indicator on catalog and install cards. The **invitation email** joins the gate's exception list (admin-initiated, same reasoning as the new-account email) and **accepting an invitation confirms the address**. The doc's old claim that password recovery still 404s for unknown addresses is corrected: it does not, and every refusal there is now a silent no-op | [business logic](application/auth/email_confirmation.md) \| [tech](application/auth/email_confirmation_tech.md) |
| user_2fa | Optional per-user two-factor authentication — WebAuthn passkeys and TOTP; single-use recovery codes; trusted-device skip ("Do not ask on this device" for 1/7/30 days, bcrypt-hashed token, wipe-on-disable); step-up re-auth for destructive mutations; login challenge discriminated union (`LoginToken \| MfaChallenge`) shared by password and Google OAuth paths; last-factor auto-disable (removing the final factor turns 2FA off automatically); inline-error wave UX on the login challenge TOTP form | [business logic](application/user_2fa/user_2fa.md) \| [tech](application/user_2fa/user_2fa_tech.md) |
| user_roles | Three-value `UserRole` enum (`agent-user`, `agent-developer`, `admin`) layered on top of `is_superuser`. `agent-user` is the default and gets Configuration, Credentials, Environments, and Integrations tabs on install detail pages (Integrations shows only the MCP Connectors card in simplified form); `agent-developer` unlocks agent CRUD, building-mode sessions, publishing, sync-prompts; `admin` is paired with the existing superuser tier. The default role for new non-superuser accounts is `ServerConfig.default_user_role`, edited on **Admin → Server Configuration → Access** (the `DEFAULT_USER_ROLE` env setting is now seed-only). Promote / demote from the Edit User dialog on Admin → Users; `USER_ROLE_CHANGED` WebSocket event re-routes the affected user on demote. The role is also the selector for auto-provisioned company AI credentials at account creation — and a role change deliberately never re-provisions, so promotion/demotion grants and revokes no keys. The **invite wizard** picks the role at invite time from the same three-value dropdown, and `role="admin"` implies `is_superuser` (derived server-side, never accepted as a separate field, because `create_account` would otherwise accept `role="admin", is_superuser=False` and break the invariant) plus auto-confirmation. | [business logic](application/user_roles/user_roles.md) \| [tech](application/user_roles/user_roles_tech.md) |
| ssh_keys | User SSH key management for private Git repository access | [business logic](application/ssh_keys/ssh_keys.md) \| [tech](application/ssh_keys/ssh_keys_tech.md) |
| knowledge_sources | Admin-only Git-based knowledge sources with article indexing, embeddings, and semantic search. Public sources are automatically available to all users' agents; private sources are owner-only | [business logic](application/knowledge_sources/knowledge_sources.md) \| [tech](application/knowledge_sources/knowledge_sources_tech.md) |
| user_workspaces | Workspace isolation for organizing agents, credentials, sessions by context | [business logic](application/user_workspaces/user_workspaces.md) \| [tech](application/user_workspaces/user_workspaces_tech.md) |
| theming | Client-side, per-device UI personalization on two orthogonal axes — color **mode** (light/dark/system, via `.dark` on `<html>`) and visual **skin** (`default` / `hacker-1980`, via `data-skin` on `<html>`). A skin is a pure CSS-variable overlay across three tiers (shadcn semantic tokens, Tailwind v4 palette scales, decorative `--skin-*` tokens) with no component-level opt-in. Settings → Interface → "Theme and Colors" card: mode selector, skin picker with light/dark preview swatches, `prefersMode` nudge, `experimental` badge. No backend model — persisted in `localStorage` only | [business logic](application/theming/theming.md) \| [tech](application/theming/theming_tech.md) |
| email_integration | Since Phase 4 of the channels & identity unification refactor, **email is a Server Channels transport** (`channel_type="email"`, a `PolledChannelTransport`), not a per-agent integration — per-agent Email Integration is deleted outright, along with clone-per-sender isolation, task mode ("Send Answer"), and the external-customer-inbox pattern (an unknown sender is now auto-registered and sender-routed over their own agents / the auto-install catalog, exactly like Google Chat). A scheduler polls the configured mailbox once a minute (no webhook, no webhook token); sender identity is the `From:` header, which is spoofable — a materially weaker trust tier than Google Chat's signed JWT, now feeding the same whitelist/routing pipeline. Recipient validation still guards a shared IMAP inbox; every arrival is durably recorded (`email_message`, `agent_id` nullable) before whitelist/policy/routing runs, since a polled transport's denials are silent to the sender and this row is the only lasting trace of them. | [business logic](application/email_integration/email_integration.md) \| [tech](application/email_integration/email_integration_tech.md) |
| mail_servers | Admin-owned, server-scoped IMAP/SMTP configuration (Admin → Server Configuration → Mail Servers, superuser-only) — `mail_server_config.user_id` was dropped in Phase 4. Referenced by id from an email channel's `config`, the way a Google Chat channel references its service account. New deletion guard: a mail server still referenced by any channel cannot be deleted (409, lists the referencing channels) — replaces the old per-agent-integration guard. | [business logic](application/email_integration/mail_servers.md) \| [tech](application/email_integration/mail_servers_tech.md) |
| email_sessions | What survives per-agent Email Integration's deletion: thread-root Message-ID threading (`References[0]` → `In-Reply-To` → own id, never the latest) and the durable `OutgoingEmailQueue` outbound path — the one channel transport with retry-backed delivery instead of Server Channels' usual best-effort. Session modes (`install`/`owner`) and processing modes (`new_session`/`new_task`) are gone; routing is entirely Server Channels' shared two-pass model now. Known gap: the `email_subject` session-context enrichment is dead code on a channel session (gated on `integration_type=="email"`, but a channel session is always `channel_email`) — the subject still reaches the agent through the message text. | [business logic](application/email_integration/email_sessions.md) \| [tech](application/email_integration/email_sessions_tech.md) |
| a2a_protocol | Agent-to-Agent protocol, JSON-RPC, task-based integration | [business logic](application/a2a_integration/a2a_protocol/a2a_protocol.md) \| [tech](application/a2a_integration/a2a_protocol/a2a_protocol_tech.md) \| [v1 support](application/a2a_integration/a2a_protocol/a2a_v1_support.md) |
| a2a_access_tokens | Scoped JWT tokens for external A2A client authentication | [business logic](application/a2a_integration/a2a_access_tokens/a2a_access_tokens.md) \| [tech](application/a2a_integration/a2a_access_tokens/a2a_access_tokens_tech.md) |
| mcp_integration | Agent exposure as MCP server, OAuth 2.1, exact-user ACL with email fallback, connector-scoped direct access tokens (bypass OAuth for account-less clients), connector setup | [architecture](application/mcp_integration/agent_mcp_architecture.md) \| [implementation](application/mcp_integration/agent_mcp_connector.md) \| [setup](application/mcp_integration/mcp_connector_setup.md) |
| agent_to_agent_mcp_connector | Agent-to-Agent MCP Connector — lets a consumer agent connect to another platform agent (or any external MCP server) as a first-class MCP provider injected into the SDK runtime. Producer side: `is_agent_to_agent` connector flag + user-ID ACL + connector-scoped direct token, developer-gated. Consumer side: `CredentialType.MCP_PROVIDER` credential (agent2agent / fixed_token / oauth_dcr / none), per-mode applicability, never written to `credentials.json` — injected into OpenCode `opencode.json` `"mcp"` block and Claude Code `options.mcp_servers` via `user_mcp.json` baseline + live `POST /config/mcp-servers` route. Backend-side DCR + OAuth/PKCE + pre-stream refresh for external MCP servers; SSRF/egress guard (`RD-6`); `MCP_SERVER_CONTAINER_URL` rewrite for container-reachable endpoints. Full `CredentialShare` support; use-only for `agent-user`. | [business logic](application/mcp_integration/agent_to_agent_mcp_connector.md) \| [tech](application/mcp_integration/agent_to_agent_mcp_connector_tech.md) |
| app_mcp_server | Universal MCP endpoint that routes messages to the right agent via AI classification, automatically stripping routing prefixes ("ask cinna to...") so agents receive clean task messages. As of Phase 5 of the channels & identity unification, App MCP is a singleton `ServerChannel` (`channel_type="app_mcp"`, the platform's `authenticated` transport) — the entire `AppAgentRoute` family (routes, assignments, personal routes, `is_auto_managed`, backfill-on-demand, the install-time conflict toast) is deleted, with no data migration. Reachability is automatic: every agent the caller owns with a router trigger prompt or example prompts is a candidate (`ChannelCandidateProvider`, shared with Server Channels), plus their enabled identities (`IdentityCandidateProvider`, opt-in via `allow_identity_routing`, **off by default and never inherited — every existing user loses identity-contact reach until they opt back in**). Admin gains a kill switch, `visibility` + grants, and a default agent scope via the same `ChannelPolicyService` every channel uses; availability is checked at token use (not issue), cached per user for `APP_MCP_AVAILABILITY_CACHE_TTL_SECONDS` (default 45s, fail-closed) — an admin revoke takes up to that long to bite. Agent Integrations tab MCP Connectors "New" dialog dropped both "App MCP Server Integration" and "Identity MCP Server Integration", leaving Direct MCP Connector and (developer-only) Agent to Agent MCP Connector; `agent-user` role sees a purely explanatory MCP Connectors card (trigger-prompt mirror, no toggle — there is nothing left to toggle per agent) on install detail pages | [business logic](application/app_mcp_server/app_mcp_server.md) \| [tech](application/app_mcp_server/app_mcp_server_tech.md) \| [prompt examples](application/app_mcp_server/prompt_examples.md) \| [prompt examples tech](application/app_mcp_server/prompt_examples_tech.md) |
| identity_routing | Person-level routing candidate at the **routing layer**, available to every surface rather than sitting on top of App MCP — users expose themselves as a routable identity; callers address people by name and two-stage routing (person resolution → agent selection, `match_method` `only_one`/`ai`; glob matching deleted, and as of Phase 5 `IdentityAgentBinding.message_patterns` is dropped as a column entirely) handles the rest; sessions run in the identity owner's space. Two consumers: App MCP Stage 1, and Server Channels Pass 1 (since Phase 3 of the channels & identity unification, gated on the sender's own per-channel `allow_identity_routing` opt-in — default off, never inherited). On the channel path Stage 2 returns an `IdentityGrant` re-verified in full (six conditions) at ingest, the thread binding stays the **sender's** while the session becomes the **owner's**, and `integration_type` stays `channel_<type>` so the reply still delivers. **As of Phase 5, binding creation moved entirely to Settings → Channels → Identity Server card** ("Add Agent" button) — the agent's Integrations tab MCP Connectors dialog no longer offers an "Identity MCP Server Integration" option at all. | [business logic](application/identity_routing/identity_routing.md) \| [tech](application/identity_routing/identity_routing_tech.md) |
| realtime_events | WebSocket event bus system, frontend-backend-agentenv streaming | [event bus](application/realtime_events/event_bus_system.md) \| [streaming](application/realtime_events/frontend_backend_agentenv_streaming.md) |
| plugin_marketplaces | Admin-managed Git-based plugin catalogs. Sync is a **metadata-only** operation: the repo is temp-cloned, `.claude-plugin/marketplace.json` is parsed, git coordinates (URL, commit hash, subdir) are written to Postgres, and the clone is discarded. No persistent backend cache. The stored coordinates are what agent containers use at install time to fetch plugin files themselves. | [business logic](application/plugin_marketplaces/plugin_marketplaces.md) \| [tech](application/plugin_marketplaces/plugin_marketplaces_tech.md) |
| admin_agent_environments | Superuser-only console at /admin/agent-envs — lists every AgentEnvironment across the fleet with owner, template, staleness (current vs expected image tag), model health, bundle install status, and in-use flags; supports targeted and bulk rebuild with real-time status updates via WebSocket; writes SecurityEvent audit rows for every admin-triggered rebuild; a Bundle column (bundle ID, installed version, amber `→ vX` badge when behind) and an `update_available` filter surface which environments are running a stale bundle revision — a third, independent staleness axis from image-tag staleness, remediated by apply-update rather than rebuild | [business logic](application/admin_agent_environments/admin_agent_environments.md) \| [tech](application/admin_agent_environments/admin_agent_environments_tech.md) |
| agent_activities | Activity feed, event logging, session summaries, sidebar bell indicator | [business logic](application/agent_activities/agent_activities.md) \| [tech](application/agent_activities/agent_activities_tech.md) |
| getting_started | New user and new instance onboarding — API key gate, Getting Started Modal, Rotating Hints | [business logic](application/getting_started/getting_started.md) \| [tech](application/getting_started/getting_started_tech.md) |
| server_configuration | Admin-configurable server-wide settings on `/admin/server-configuration`, split across four tabs: **Interface** (the Disclaimer card and the [Local Agent Kit](application/local_agent_kit/local_agent_kit.md) publish toggle), **Access** (a two-column grid of the `RegistrationCard` / `SignInMethodsCard` / `NewUserDefaultsCard` / `LandingPageCard` front-door-policy cards, and `CompanyAiCredentialsCard`, all half-width with icon titles), **Channels** (`ServerChannelsCard` + `AutoInstallAgentsCard` + `ServerDebugToolsCard` — see the `server_channels` row below), and **Mail Servers** (`MailServersCard`, a peer of Channels rather than part of it, since an email channel references a mail server by id and the servers outlive any one channel). Documented here: **Disclaimer** — superuser enables/disables a Markdown notice shown at login in a non-dismissible blocking modal; display mode controls whether it appears once per browser (`new_users`) or once per session (`every_login`); versioned acknowledgement in browser storage means any content or mode edit re-shows the disclaimer to all users. **Access Policy** — six `ServerConfig` columns deciding who may get an account and how they sign in: `registration_mode` (`open` / `invite_only`), `allowed_email_patterns` (comma-separated globs; empty and `*` both mean no restriction; gates registration only, never login), `password_auth_enabled` (Google-only mode, with superusers keeping password login as a break-glass), `google_auto_register` (ignored in invite-only, where Google never creates an account), `default_user_role`, and `invite_include_desktop_default` (which pre-ticks the invite wizard's desktop checkbox and is the server-side fallback when the request omits it; the chosen value is then stored on the invitation row, and the public accept page reads it from there, never from the policy). `AccountOrigin.INVITE` is in the ungated set, so an administrator may invite an address outside the pattern list and in invite-only mode — the pattern list bounds self-service registration, not administrator intent, and the wizard deliberately does not re-implement the match client-side. Replaces the `AUTH_WHITELIST_USER_DOMAINS` and `DEFAULT_USER_ROLE` env settings, which become first-boot seeds with a startup warning. Server-side lockout validation on `PUT /admin/server-config` returns bare reason codes; a public, rate-limited `GET /server-config/access-policy` projection (no patterns, no role) drives `/login` and `/signup`. Its own **Company AI providers** card (one row per AI Provider with a role toggle group, capped at 5 rows, link to `/admin/ai-credentials#providers`) edits `AIProvider.auto_provision_roles` rather than any `ServerConfig` column — the card moved with the rule when ai-credential-providers took it off the credential; see the `admin_ai_credential_provisioning` row. **Public Landing Page** (phase 4) — `ServerConfig.landing_markdown`, admin-authored Markdown rendered on the public, unguarded `/start` route: one stable address to paste into a wiki, showing whichever sign-in methods the instance offers plus the desktop download. Served by its own public `GET /server-config/landing` projection rather than as a field on `AccessPolicyPublic` (content, not policy — `/login` and `/signup` read the policy on every load), which costs a second request from the *shared* anonymous rate-limit bucket (`ACCESS_POLICY_RATE_LIMIT_PER_MIN` 120 → 240; exhaustion is silent and renders `/start` as though the instance offers nothing). `None` = unchanged / `""` = clear, never bumps `disclaimer_version`, 16 KiB cap, and a write-path `reject_raw_html` validator. **The security boundary is the render path** — `components/Landing/LandingMarkdown.tsx` has its own plugin list (no `rehype-raw`) and runs `rehype-sanitize`, so a change to the chat renderer cannot reach `/start`; the biome restricted-import rule is a signpost only (no CI), and nothing in this repo runs frontend tests. `AccessPolicyPublic` also gained `password_signup_available`, the server-side *answer* `/login`, `/signup` and `/start` render from instead of each recombining `registration_open` and `password_auth_enabled` | [disclaimer](application/server_configuration/disclaimer.md) \| [disclaimer tech](application/server_configuration/disclaimer_tech.md) \| [access policy](application/server_configuration/access_policy.md) \| [access policy tech](application/server_configuration/access_policy_tech.md) \| [landing page](application/server_configuration/landing_page.md) \| [landing page tech](application/server_configuration/landing_page_tech.md) |
| server_channels | Admin-configured, server-wide inbound channels that let people outside the platform (starting with Google Chat) message platform agents. The webhook is platform-unauthenticated by design; defenses are an unguessable per-channel `webhook_token` (404 on unknown *or* disabled — no existence leak), adapter JWT verification as the sole authentication chokepoint (fails closed), rate limiting, a body-size cap, and a fail-closed comma-separated `email_whitelist` — entries are case-insensitive `fnmatch` globs (`*`, `?`, `[seq]`), matched against the whole address by `app/services/common/email_patterns.py`, and a sender is admitted when *any* entry matches. Null/empty denies everyone; any entry that matches every address (`*`, `*@*`, …) is a blanket allow, so `"*, ops@corp.com"` admits everyone. Whitelisted-but-unknown senders are auto-registered as passwordless, email-confirmed `agent-user` accounts when `auto_register_users` is on — the channel's own whitelist is the sole registration gate — `create_external_user` uses the `external` registration origin, so the instance [access policy](application/server_configuration/access_policy.md) (invite-only mode, allowed email patterns) is deliberately not re-checked (precedent: email integration; shared `UserService.create_external_user` helper). **Admin-owned availability policy, resolved per user by `ChannelPolicyService` (single chokepoint, plain-data result — never re-derived by the router, the API, or the frontend):** each channel carries four defaults (`visibility` public/restricted + `server_channel_user_grant` allowlist, `default_enabled_for_users`, `default_agent_scope` all/list/none, `allow_auto_install`) that a user may override per-channel from Settings → Channels (`channel_user_setting`, `channel_user_agent`); absence of that settings row means "the channel default applies" — materialized lazily, on first edit, never at channel-create or account-create time, since auto-registered senders and later-created users would otherwise never get one. `is_enabled`/`agent_scope` are nullable-meaning-inherit (a stored value would freeze the user against a later admin-default change); `allow_identity_routing` is the deliberate exception — `NOT NULL DEFAULT false`, never inherits (an admin default must not consent to routing into someone else's workspace on the user's behalf), and it is the **sender's own** consent, not the receiver's gate. Since Phase 3 it is read on every message and surfaced in Settings → Channels (master switch + the list of people who shared an identity, reusing the person-level `IdentityBindingAssignment.is_enabled` toggle rather than a second per-channel allowlist), with transitions audited as `SERVER_CHANNEL_IDENTITY_ROUTING_CHANGED` (severity `medium`). **Identity routing (Phase 3):** with the switch on, Pass 1's ballot also carries one candidate per identity owner the sender may address; a person winning hands off to identity Stage 2, which picks one of *that person's* agents and returns an `IdentityGrant` whose six conditions are re-verified against the database on every message. The `ChannelThreadBinding` stays owned by the **sender** (whose thread is this) while `session.user_id` becomes the **identity owner** (whose workspace is answering), and `integration_type` stays `channel_<type>` — the prefix `ChannelOutboundService` gates reply delivery on. Turning the switch off, or a revoked grant, declines the next message on an already-bound identity thread with the same generic reply every refusal gets; the binding then fails and self-heals, re-routing over the sender's own agents. An identity the sender has *not* opted into is deliberately absent from the trace entirely (the one inversion of the never-drop-silently rule — recording it would enumerate other people's identities to an external sender). A revoked sender (admin disable, withdrawn grant, or the user's own toggle) is declined on an *existing* bound thread exactly as on a new one, with the same detail-free reply a whitelist miss gets — indistinguishable from every other denial cause, or the reply becomes an oracle for a server's channel configuration. A user may also pin one of their own agents to a channel (`channel_user_setting.pinned_agent_id`, ownership re-checked at resolution since the FK only guarantees existence): classification is skipped entirely, the pin gets its own trace row (`match_method="pinned"`), and a pinned channel never auto-installs even when the pin fails to resolve. Two-pass routing per new thread: a pin is answered first; otherwise Pass 1 builds candidates directly from the sender's own agents (`Agent.owner_id == user_id`), narrowed to the resolved agent scope (an out-of-scope owned agent is a skipped candidate, `SKIP_NOT_IN_CHANNEL_SCOPE`, checked before wording) — eligible when in scope and a router trigger prompt or example prompts are set, an owned in-scope agent with neither recorded as a skipped candidate rather than dropped — and reads no `AppAgentRoute`, assignment, or `channel_app_mcp` flag, sharing only `AgentClassifier.classify` with the App MCP Server (routing_tuning's Phase 5 classifier unification); a conditional `only_one` short-circuit routes without a model call when exactly one agent is eligible and the auto-install catalog has nothing else to offer this sender. Pass 2 over a server-wide auto-install bundle list, classified the same way and gated by `CatalogService.user_can_install` (list membership is not an implicit grant), **runs only when the resolved agent scope is `"all"`** — not merely when `allow_auto_install` is on — because installing a bundle whose agent is out-of-scope by construction would create a real side effect on a path guaranteed to dead-end; a decided product semantic, argued in [business logic](application/server_channels/server_channels.md#availability-and-per-user-settings). `ChannelThreadBinding` pins one external thread to one `(user, agent, session)`: `pending_install` → `active`; `failed` self-heals by deleting the binding and re-routing on the next message; a non-owner posting into a bound thread, or the loser of a binding-creation race, is declined rather than routed. New `SessionSender` kind `channel_caller` (session owner is always the sender's own account, same trust shape as `task_executor`). `channel_pending_scheduler` (TESTING-gated, 45s interval) flushes parked messages once an auto-installed environment reaches `running` — a `critical_state` environment does NOT fail the binding, since it is running-but-degraded and would have answered (diverges from the original plan, which predates that semantic). Google JWKS verification hardening (`GoogleCertsUnavailable`, deliberately not a `ValueError`) was fixed at the shared cert-fetch helper, so the pre-existing Google OAuth login path gets it too. Outbound delivery (`STREAM_COMPLETED`/`STREAM_ERROR` → adapter `send_message`) is best-effort for a webhook transport — 3 retries, no persistent queue. **Progress narration is one message, not several:** a transport that can edit its own posts (`ChannelCapabilities.supports_status_notice` = progress updates + edit; Google Chat via `spaces.messages.patch`) gets a single **status notice** posted into the thread when the work starts, rewritten in place as it advances (routing → installing → ready → working) and rewritten one last time to hold **the agent's reply itself** (`_deliver(into_status_notice=True)` → `replace_message`, chunked, first chunk taking the slot), with `ChannelThreadBinding.status_message_id` carrying it across the webhook → install → scheduler-flush hop and **released once the reply has taken the notice's slot** — including when the reply lands truncated because the chunks after the first failed to post, since a retained id would let the next turn's spinner be patched over an answer that already went out. Text that IS the answer ("nothing matched", "setup failed") *settles* the notice instead, releasing the id so the message stays. Deletion (`supports_message_delete`, deliberately not part of the derived capability) survives only for the two turns with nothing to put in the slot: Chat renders a deleted message as a "Message deleted by its author" tombstone, so clearing the notice after posting the reply beneath it put one above every single answer. The consequence at the webhook: an **accepted** new-thread message now acks with an empty `{}` body **on a channel that also has outbound credentials configured** — Chat creates a synchronous reply but never returns its id, so a notice answered inline could be neither rewritten nor removed. A channel with no outbound credentials configured keeps the old synchronous "finding an assistant…" ack instead, since the notice provably cannot be posted and answering with nothing would be worse. Declines stay synchronous in every case, which is what lets a channel refuse a sender before its outbound credentials exist. **The notice also streams (Google Chat):** while the agent writes, `ChannelStreamRelay` tees off the session's own stream handler (`CompositeStreamEventHandler`, primary + isolated passengers) and rewrites the notice in place on a debounce (`CHANNEL_STREAM_UPDATE_INTERVAL_SECONDS`, default 3.0; `<= 0` means flush on every event), **sealing** the draft into a standing message at a paragraph/newline boundary — never inside a fence, never between table rows (`adapters/chat_text_chunking.find_seal_boundary`) — once its *translated* length passes `CHANNEL_STREAM_SEAL_TARGET_CHARS` (3400, under Chat's 4096 cap), then opening a fresh draft below it; the final delivery patches only the tail. The relay sends **raw markdown** (translation stays in the adapter; `markdown_to_chat` is used only to *measure*), only `assistant` events feed it (`thinking` never leaves the platform), and it strips the agent's `<webapp_action>` / `<cinna_attach>` tags itself since the settled reply is the relay's own text rather than the stored `SessionMessage` (a decided divergence, mismatches only logged). Everything is gated on `supports_status_notice`, so email and App MCP are untouched, and every failure degrades to today's one-shot delivery (`CHANNEL_STREAM_UPDATES_ENABLED` is the kill switch). Relay state is in-memory and legitimately so — relay, stream and handlers share one process and one task lifetime. `STREAM_INTERRUPTED` is now subscribed too (emitted *instead of* `STREAM_COMPLETED` for a stopped turn), which is what gives the new **`/stop` channel control command** its acknowledgement: an extensible registry (`channel_control_commands.py`, exact match only) intercepted at inbound step 7a — strictly after the thread-ownership decline and before binding dispatch, so it inherits every security gate — success is silent (the settled stopped marker IS the ack), "nothing running" is the only reply, and a `/stop` as a thread's first message, or one carrying attachments, is an ordinary message. No migration, no client regeneration. Every synchronous reply now carries `thread.name`: Chat posts an unthreaded sync response as a new top-level message in the *space*, which is why the pipeline's first word used to land outside the conversation every later message went into. Outbound text is translated from CommonMark to Chat's own markup (`adapters/google_chat_format.py`) because `spaces.messages.create` has **no** `markupSyntax` parameter — the `MARKUP_SYNTAX_CHAT`/`MARKUP_SYNTAX_MARKDOWN` enum is read-side only, on `spaces.messages.get`. **Phase 4 of the channels & identity unification** split the adapter contract into transport shapes (`ChannelCapabilities.inbound_mode`: `webhook` / `polled` / `authenticated`, plus `needs_webhook_token` / `needs_outbound_credentials`) so a pull transport is no longer forced through a webhook-shaped ABC, and added **email** as the first `polled` channel (`EmailChannelAdapter`, `PolledChannelTransport`): a `TESTING`-gated scheduler (`channel_poll_scheduler`, 60s) polls every enabled polled channel and feeds messages into the same post-verification pipeline a webhook reaches after `verify_inbound`; `server_channel.webhook_token` is nullable for exactly this reason. Email's outbound is the one exception to "no persistent queue" — it routes through the pre-existing, durable `OutgoingEmailQueue` instead of the best-effort adapter retry path, and its outbound credential is derived from the referenced `MailServerConfig` (`needs_outbound_credentials=False`) rather than `ServerChannel.encrypted_secrets`. Per-agent Email Integration is deleted outright in the same phase — see the `email_integration` row above. **Phase 5 of the channels & identity unification** added the third transport shape, `authenticated`, and its sole occupant: **App MCP is now a singleton `ServerChannel`** (`channel_type="app_mcp"`, `is_singleton=True`, enforced by both `ServerChannelService` and a partial unique index `uq_server_channel_singleton_type` on `channel_type`). No config, no secrets, no whitelist — the caller already holds a platform bearer token bound to one `user_id`, so `needs_webhook_token=False` and `needs_outbound_credentials=False` (its `has_outbound_credentials()` always returns `True`: the reply *is* the synchronous MCP response, so there is nothing to be missing). Availability is checked at token use, not issue: `AppMCPTokenVerifier` resolves `ChannelPolicyService.resolve(...).is_available` on every request, cached per user for `APP_MCP_AVAILABILITY_CACHE_TTL_SECONDS` (default **45s**, fail-closed — a lookup error denies and is never cached). The entire `AppAgentRoute` family (admin routes, per-user assignments, personal routes, `is_auto_managed`, backfill-on-demand, `message_patterns`) is deleted in the same phase, with no data migration; see [App MCP Server](application/app_mcp_server/app_mcp_server.md). **Phase 6 of the channels & identity unification** made trace and debug coverage uniform across the three transports: email decisions now carry their own routing-trace `origin="email"` (they were written as `server_channel` before, and rows predating the change keep that value), App MCP writes routing traces for the first time (`origin="app_mcp"` with a populated `channel_id`, behind `ROUTING_TRACE_APP_MCP_MODE`'s metadata-only default), and `google_chat` keeps `server_channel` deliberately. The live debug feed is unchanged and already covered email — the poller feeds the same `ChannelInboundService.process_inbound` a webhook reaches, where every inbound debug hook lives; **App MCP emits no debug-buffer events at all, by design**, since its reply *is* the synchronous MCP response and there is no `ChannelOutboundService` path to hook. The singleton guard has two layers: `ServerChannelService.delete_channel` raises `UnsupportedChannelOperationError` (a delete would silently re-materialize the row with default settings, i.e. reset the kill switch to on — disabling is the operation an admin actually wants) and the partial unique index `uq_server_channel_singleton_type` backs it at the database. **Inbound file attachments (both live channels — Google Chat, email):** a person can attach files to a channel message and they reach the agent's workspace by the identical mechanism a web-chat upload uses — a transport declares `supports_inbound_attachments`, `ChannelAttachmentService.materialize` turns refs into ordinary `FileUpload` rows owned by the **sender** (not the session owner — the one deliberate ownership widening, load-bearing for identity-routed threads and bounded by an architecture-test-pinned caller set on `uploader_user_id`), and `file_ids` ride beside `text` through routing, parking and ingest with no schema change, no migration, and no OpenAPI client regeneration. Materialization runs as step 6.5, strictly after every admission gate (rate limit, verification, dedup, whitelist, user resolution, channel policy) and before routing, a property two architecture tests pin. No attachment failure can fail a message — a rejection is a per-file skip, named in the stored transcript and in the admin debug feed's `attachments_accepted`/`attachments_skipped`/`attachment_skips` fields (grouped into Refused/Needs-resending/Failed families in the admin panel). There is deliberately no admin `allow_attachments` switch — the MIME allowlist, size/count/aggregate caps and the sender's own storage quota already bound the risk a switch would exist to close. Google Chat fetches are hardcoded to the Chat media host with `resourceName` shape validation, the platform's egress guard on every hop, `https`-only, and a redirect rule that retains the bot's bearer token same-origin but strips it (via a one-way latch) the moment a hop leaves that origin; Drive-linked attachments are never fetched. Email keeps the attachment bytes its MIME parser already decodes, excludes `cid:`-referenced inline parts (a documented trade that also drops some pasted-screenshot cases), enforces a per-poll-tick byte budget, and delivers a forwarded message's inner attachments loose, falling back to a materialized `.eml` only when nothing else was deliverable. See [business logic](application/server_channels/server_channels.md#inbound-file-attachments) \| [tech](application/server_channels/server_channels_tech.md#inbound-file-attachments). Adding a channel type is a new adapter module + one registry entry, no migration. Admin UI: Channels tab on `/admin/server-configuration` — channel CRUD with a write-only secret field, a setup-instructions panel (webhook URL, test-outbound targeted by email or raw thread id, token regeneration), a per-channel debug dialog (bug icon) showing an in-memory, process-local live feed of recent inbound/outbound events with a "Reply here" test-send action (cleared on backend restart), and the auto-install list with visibility / missing-trigger-prompt amber badges. The webhook URL is built from `BACKEND_BASE_URL` (former name `WEBHOOK_BASE_URL` still honoured; falling back to `FRONTEND_HOST`) — a single resolution point (`settings.backend_base_url`) shared with task-trigger and agent-webhook URLs, the Agent REST API base, A2A attachment links and native-client OAuth discovery, since a split-host deployment serves the SPA and API on different origins; `make webhook-tunnel` / `webhook-set-url` / `webhook-clear-url` support local testing against a real provider. | [business logic](application/server_channels/server_channels.md) \| [tech](application/server_channels/server_channels_tech.md) \| [debug monitor](application/server_channels/channel_debug_monitor.md) \| [debug monitor tech](application/server_channels/channel_debug_monitor_tech.md) |
| routing_tuning | Makes every LLM-mediated routing decision (Server Channels, email and App MCP as of Phase 6 of the channels & identity unification; `identity` stays a reserved origin because identity routing is a *stage* inside another origin's decision, never a decision of its own) observable, durable, reproducible, and advisory — replacing what used to be a one-line entry in a process-local in-memory ring buffer. A `routing_decision` row per decision: every candidate considered including rejected ones with a `skip_reason` (single highest-value field on the trace), the rendered classifier prompt, raw model response, provider/model attempts, verdict, latency. Backend-computed **reachability verdict** (**16** codes, one sentence + remedy each, tested sentence-by-sentence — down from 22 before Phase 5 of the channels & identity unification, which deleted six App-MCP-only route-based codes outright rather than merging them — the docs said 21→15 for a while, and both halves were off by one) is the headline, split by origin for *wording only*, never for the underlying finding — as of Phase 5, a channel decision and an App MCP decision are diagnosed by the same function (`_verdict_from_configuration`) against ownership and wording (e.g. an agent unreachable because it has neither a router trigger prompt nor example prompts); there is no route in the story on either surface any more. Superuser-only **simulate** (no side effects by construction — a pure `ChannelRoutingService.decide()`, not a flag), **replay** (re-run + diff), and **recommendation draft** (copyable trigger-prompt wording, writes nothing — the admin surface is read-only with respect to agents). `ROUTING_TRACE_STORE_MESSAGE_TEXT` gates the sender's own words by an allowlist of fields declared safe, not a list of tainted ones, applied identically to the write and read paths; hides, does not erase — only retention expiry or an explicit trace-clear removes stored text. `ROUTING_TRACE_RETENTION_DAYS` must be `>= 1` or exactly `-1` (keep forever); `0` is rejected. Phase 5 has shipped: one `AgentClassifier` now backs all four routing consumers, `prompt_examples` reaches the LLM (measured 17/36→33/36 intended-target hits, zero regressions, one documented lateral move), confidence/reason/runner-up are parsed defensively and recorded but never gate routing, and identity Stage-2 candidate capture is complete and correct in code (`_binding_candidates`/`_record_candidates`/`record_match` in `IdentityRoutingService`). **Stage-2 capture became reachable in Phase 3 of the channels & identity unification** — `ChannelRoutingService.decide` opens the `identity_stage2` stage from inside channel Pass 1's capture, so a channel decision durably records the identity candidates (`ref_id` = `identity:{owner_id}`), any `SKIP_IDENTITY_UNAVAILABLE` skips, and the whole Stage-2 sub-decision. **Phase 6 of the channels & identity unification closed the trace gaps.** `RoutingSimulateRequest` now carries an optional `channel_id`, so a hand-typed simulate can decide under a real channel's policy and put an identity candidate on the ballot (a channel id that resolves to nothing is a 404, refused before the run spends an LLM call). App MCP writes its own traces (`origin="app_mcp"`, populated `channel_id`) behind `ROUTING_TRACE_APP_MCP_MODE` (`off` / `metadata` / `full`, default `metadata` — App MCP routes *every* message and sits behind no webhook rate limit, so it is the one origin whose write volume is unbounded; an unknown value is rejected at startup). Email decisions now emit `origin="email"` instead of `server_channel` — a deliberate value change on a live surface, so rows written before Phase 6 still carry the old value and "every email trace" and "every `origin=\"email\"` trace" are different sets; `google_chat` deliberately keeps `server_channel`. The four live origins are therefore `server_channel` / `app_mcp` / `email` / `simulate`; `origin="identity"` remains reserved and unemitted **by decision**, not as a skipped item. Remedy wording is now chosen by an origin→profile map with an explicit generic default, so an unknown origin degrades to origin-neutral wording rather than to App MCP's. `?expected_agent_id=` accepts `identity:{owner_id}` as well as an agent UUID, so a verdict can name a *person* — no admin UI control sends it yet (Phase 7). `stages[].not_run_code` is a closed, server-authored vocabulary (`pinned` / `channel_scope` / `auto_install_off` / `simulate_toggle`) that survives the message-text gate, so "Pass 2 never ran, and here is which switch" stays diagnosable with the gate closed. Routing-tuning's own Phase 6 (curated eval set) remains unbuilt. | [business logic](application/routing_tuning/routing_tuning.md) \| [tech](application/routing_tuning/routing_tuning_tech.md) |
| chat_windows | Chat window rendering across session pages, guest shares, webapp widgets, and dashboard prompt actions — markdown, tool blocks, streaming display, auto-scroll | [business logic](application/chat_interface/chat_windows.md) \| [tech](application/chat_interface/chat_windows_tech.md) \| [tool rendering](application/chat_interface/tool_rendering.md) \| [tool tech](application/chat_interface/tool_rendering_tech.md) \| [markdown](application/chat_interface/markdown_rendering.md) \| [auto-scroll](application/chat_interface/auto_scroll_and_streaming_display.md) \| [ask user question](application/chat_interface/tool_answer_questions_widget.md) \| [tool approval](application/chat_interface/tool_approval_widget.md) \| [webapp widget](application/chat_interface/webapp_chat_widget.md) \| [file sending](application/chat_interface/file_sending_and_ui.md) \| [dashboard prompt actions](application/chat_interface/dashboard_prompt_actions.md) \| [dashboard prompt actions tech](application/chat_interface/dashboard_prompt_actions_tech.md) |
| user_dashboards | Customizable grid-based monitoring dashboards — per-user, workspace-independent, agent blocks with webapp/session/tasks views, and hover prompt actions that execute in-place with streaming display, session reuse, and webapp action forwarding | [business logic](application/user_dashboards/user_dashboards.md) \| [tech](application/user_dashboards/user_dashboards_tech.md) |
| cinna_cli_integration | Local development CLI — setup tokens, continuous Mutagen-based bidirectional workspace sync over a WebSocket tunnel, foreground `cinna dev` TUI, per-user `~/.cinna/agents.json` registry, remote exec streaming, MCP knowledge proxy, building context with inline `prompt_files` companions; credentials stay on the platform. `GET /api/v1/cli/git-coordinates` (agent-scoped CLI token) exposes VCS repo coordinates to the CLI for sparse-checkout git-linked local dev (planned cinna-cli side; no deploy key in the response) | [business logic](application/cinna_cli_integration/cinna_cli_integration.md) \| [tech](application/cinna_cli_integration/cinna_cli_integration_tech.md) \| [local dev](application/cinna_cli_integration/local_cli_development.md) |
| account_cli_workspace | Account-level CLI workspace (Phases 1–5) — one `curl \| python3` bootstrap from Settings → Security creates an account workspace; `can_build`-gated child-token minting lets a local coding agent discover agents and mint standard per-agent CLI tokens on demand; account token cascade-revokes all minted children; structural isolation prevents account tokens from reaching per-agent routes and vice versa; `GET /account/context-package` delivers a memoized gzip tarball of platform docs + generated API reference + worked guides (`context/guides/`) into the workspace `context/` tree for the orchestrator agent; Phase 3 adds `cinna agent create`, `cinna connect agent-api`, `cinna connect mcp` convenience verbs (wrapping existing connect helpers), `cinna agent-api enable|refresh|spec` to build+verify a producer's REST API before connecting it, and a policy-guarded `POST /account/api-proxy` escape hatch for generic authenticated API access (denylist: credentials, user-management, admin, CLI recursion, auth, MFA, app-sync; 120/min rate limit); an **active user-workspace** setting (`cinna account user-workspace list|--activate`, stored client-side) so CLI-created agents (and the credentials they inherit) land in the chosen workspace; **secrets-safe credential drafting** (`cinna account credentials list|create|update|delete|share-with-agent`) where the local agent scaffolds empty credential drafts and attaches them to agents but can never read or write secret values — the user fills those in the UI; Phase 4 ships the agentic-network playbook (`context/guides/build-an-agentic-network.md`) and reaches full agentic-teams CRUD via the escape hatch (no new CLI verbs or backend endpoints); also ships the prompt-authoring guide (`context/guides/authoring-agent-prompts.md`) — bulk-write the agent's six prompt fields + description in one `cinna api PUT agents/<id>` call, with automatic SEED_PUSH into the running environment and a finalize-the-description rule baked in; Phase 5 adds dedicated **schedule** verbs (`cinna agent schedule list\|generate\|create\|update\|run\|logs\|delete` — full CRUD with the UI's per-type frequency floor and foreign-install read-only contract) and **status** verbs (`cinna agent status show\|refresh\|set-command`) via thin `/account/agents/{id}/schedules*` + `/status*` endpoints that delegate to `AgentSchedulerService` / `AgentStatusService` (no new persisted models, no migration); also ships **`cinna chat`** — console-chat with optional local-file upload: `POST /api/v1/cli/account/files/upload` is a dedicated multipart route (the JSON-only api-proxy cannot carry a binary body) that delegates to `FileService.create_file_upload` and returns `FileUploadPublic`; all session control (create / send / poll / interrupt) rides the existing api-proxy (`sessions/*` is not on the exclusion denylist); the stream endpoint returns a JSON ack (polling not SSE); `GET files/{id}/download` is also proxied (binary 1:1, 8 MiB cap); no new models, migration, or security events; also ships four `/account/improvement-requests*` routes (`cinna improve list|show|download|status`, CLI verbs live in the separate `cinna-cli` repo) delegating to the shared `ImprovementRequestService` from [Agent Improvement Requests](application/agent_improvement_requests/agent_improvement_requests.md), plus the `context/guides/handling-improvement-requests.md` playbook; also ships `POST /account/desktop-token`, the Cinna Desktop silent-link exchange — the account token buys an ordinary user JWT, so it is the one account-CLI route that hands out a credential *outside* the token-type isolation; rate-limited per token, audited on success and refusal, revocable as an App Session, included in the account token's revoke cascade (which therefore counts desktop sessions in its "N session(s) disconnected" reply), and blocked from minting replacement CLI credentials or a fresh native session (the gate on the consent routes covers approve only; deny stays reachable) — revoking is still necessary-not-sufficient: the issued session is a full user JWT, and an MCP OAuth credential it mints through the ungated MCP consent outlives the cascade and every other revocation control (recorded gap, raised separately) | [business logic](application/cinna_cli_integration/account_cli_workspace.md) \| [tech](application/cinna_cli_integration/account_cli_workspace_tech.md) |
| account_device_login | `cinna login` — RFC 8628 device-authorization grant that refreshes an expired account CLI token through a browser approval instead of pasting a new setup token; CLI polls five unauthenticated + authenticated backend endpoints; poll always returns HTTP 200 with a `status` field (deliberate divergence from RFC 400+error shape); `device_code` ≥256-bit entropy stored only as SHA-256 hash, single-use, 15-min expiry; token minted at approval by any authenticated platform user (no developer-role gate), identical in scope to the `cinna account setup` token via the shared `mint_account_cli_token` helper; browser consent page at `/device` reuses the native app-approval UI (`NativeAuthConsentPage`), auth-gated, non-redirecting; feature is end-to-end complete (backend + frontend + CLI) | [business logic](application/cinna_cli_integration/account_device_login.md) \| [tech](application/cinna_cli_integration/account_device_login_tech.md) |
| local_agent_kit | Public, unauthenticated, read-only `/agent-start` (+ `/api/agent-start` alias) entrypoint that hands a local coding assistant (Claude Code, Codex, opencode, …) a versioned, platform-maintained kit — no Cinna account required. Kit content is hand-authored under `docs/local_agent_kit/`, synced into the `platform-knowledge-env` snapshot the same way feature docs are, rendered per-instance (placeholders resolved from settings only, never the request) and content-hashed into `kit_version`. The tree splits into a **contract** — `kit.json`, `layout.json`, `CONTRACT_VERSION`, `CHANGELOG.md`, `schema/**`, `templates/**`, a declared member subset (never a second tree) served as `GET /agent-start/contract.tar.gz` rooted at `cinna-contract/`, with `GET /agent-start/contract/version` beside it — which a second host (Cinna Desktop) consumes to create and validate agent folders, and **guides/tools** (`START.md`, `README.md`, `guides/`, `assistants/`, `tools/`), kit-only. Both contract representations 503 together when a load-bearing member is missing or the snapshot's three `contract_version` declarations disagree, while the kit surface keeps serving a stranger. Teaches a cloud-mirroring local agent layout (`docs/`, `scripts/`, `knowledge/`, `files/`, `config/`, `credentials/`, `app-data/`) declared as data in `layout.json`, a `cinna-agent.json` manifest carrying the same definitional metadata a bundle revision carries plus a stable `id` and the `contract_version` that is the major-version compatibility gate (the legacy integer `schema_version` is still parsed and nothing branches on it), a sibling `publications.json` ledger, and a capability ladder that adds artefacts only when a rung's trigger fires. Stdlib-only `kit.py` (`new`/`validate`/`list`/`refresh`/`export`/`chat`) scaffolds and validates agents on the user's machine and sends one prompt to an agent running under Cinna Desktop through the desktop-owned `app-data/desktop.json`; going cloud is `cinna login <host> --dir Cloud/<host>` (one account workspace per Cinna instance) + `cinna agent import ../../Local/<slug>` from inside it (cinna-cli, separate repo) — `credentials/` is never copied, only setup URLs. Per-representation ETags, IP-keyed rate limiting (socket peer / last appended `X-Forwarded-For` hop), and a `ServerConfig.local_agent_kit_enabled` instance opt-out (404, never 403, on both mounts when off) round out the surface; the kit also rides the account context package under `context/local-kit/`. Four human-facing surfaces (Getting Started article, Rotating Hints entry, Local Development card hint, login-page link) all gate on an unauthenticated probe of the kit's own `/api/agent-start/version` | [business logic](application/local_agent_kit/local_agent_kit.md) \| [tech](application/local_agent_kit/local_agent_kit_tech.md) |
| desktop_auth | Native-client App Authentication — OAuth 2.0 + PKCE flow for Cinna Desktop and Cinna Mobile; instance discovery, authorization code issuance, access + refresh token pair, token rotation with replay detection and a reuse-grace window (recovers lost rotation responses without forcing logout), per-device revocation from Settings that takes effect immediately on the next request (not just on next refresh). Mobile reuses the same service/tables/logic through a parallel `/app-auth` route surface (discovery via `/.well-known/cinna-app`, consent at `/app-auth/consent`) — only the URL namespace and native redirect schemes differ; the consent screen adapts its copy via the redirect-derived `client_kind`. A second, consent-free grant path exists for Cinna Desktop: `POST /api/v1/cli/account/desktop-token` trades a CLI **account** token found in a workshop tree for a desktop pair plus the account email (silent link), issuing through the same `DesktopAuthService` primitive so rotation/replay/revocation apply unchanged — the resulting client carries `origin="cli_exchange"` plus a link to the account token that bought it (every grant writes both through one `_stamp_grant` — the single-writer property is over grants, not over the columns, since `origin` is also seeded at row creation — and it also retires the client's previous grant when the provenance changes, so neither direction can be laundered; that retirement is scoped to the one client, so other devices stay signed in), shows a **CLI link** badge in App Sessions, audits success and refusal, and is revoked either by disconnecting it there or by revoking that account token, which now cascades into desktop sessions as well as child CLI tokens. Every desktop token response (exchange, code exchange, refresh) carries `email`, the account the tokens belong to, because a lazy-registration consent can be approved by any authenticated holder of its nonce and the redeeming app would otherwise not see whose session it holds — visibility, not prevention | [business logic](application/desktop_auth/desktop_auth.md) \| [tech](application/desktop_auth/desktop_auth_tech.md) |
| desktop_onboarding | One-click path from "an admin created your account" to a running, self-configured Cinna Desktop — no terminal, no invite token, one sign-in. The new-account email's primary button ("Get Cinna Desktop") links to the **public** `/desktop` landing page, which detects OS/arch, offers a single download button, and hands the app this instance's address via a `cinna://connect?server=…` deep link (percent-encoded by `URLSearchParams` — the desktop handler must decode; cross-repo contract) with a copyable paste-fallback beside it. `GET /api/v1/desktop/download?os&arch&kind` (public) resolves the latest `opencinna/cinna-desktop` release and **302s to the asset, never proxying the binary** — 1 h metadata cache (60 s for failures) per worker behind a single-flight lock, an `ASSET_URL_PREFIX` guard against open redirect, exactly four supported combinations (`darwin/arm64/dmg`, `darwin/x64/dmg`, `linux/x64/appimage`, `linux/x64/deb`; Linux arm64 has no published asset), `422` for an invalid enum value but a `302` releases-page fallback for a valid-but-unpublished combination and for every failure, and a `DESKTOP_DOWNLOAD_BASE_URL` mirror mode addressed by *shape* (`{base}/{os}/{arch}/{kind}`) so an air-gapped install needs no reachable GitHub. `/.well-known/cinna-desktop` gains an optional `local_dev` block (`setup_token_endpoint`, `cinna_cli_version`, `mutagen_version`), emitted only when `DESKTOP_AUTH_ENABLED` **and** `DESKTOP_LOCAL_DEV_ENABLED`; the six pre-existing keys are unchanged. Two things that read as security guarantees and are not: `DESKTOP_LOCAL_DEV_ENABLED` gates the **advertisement only** — `POST /api/v1/cli/account/setup-tokens` stays reachable and is governed solely by `require_developer` + `NoCliExchangedSession` — and the presence of `local_dev` (served to every anonymous visitor) is **not** a promise the current user may mint: an `agent-user` desktop gets a `403`, by design. The **invitation** email carries the same desktop offer as a secondary line (its primary button has to be "Accept the invitation"), gated on the invitation row's own `include_desktop` rather than on the live policy, and repeats it as a button on `/accept-invite/done`. Nothing new is minted or persisted — no models, no migration, no token types. Phase 4 of zero-touch onboarding extracted the offer itself into `DesktopDownloadSection` so the public [`/start`](application/server_configuration/landing_page.md) hub can embed it verbatim (`/desktop` renders unchanged), and repointed the new-account mail's *secondary* browser link from the bare SPA origin to `{FRONTEND_HOST}/start`; the **invitation** mail's `web_link` deliberately stays the bare host, since `/start` carries no token and cannot open an invitation. `desktop_enabled` gates the **advertisement** only — `/start` hides its download card when it is off, while `/desktop` still renders and the download resolver stays ungated, so links in already-sent emails never break. Recorded hazard: `VITE_API_URL` (SPA) and `BACKEND_BASE_URL` (backend) are independent settings for the same value and must resolve to the same origin, with nothing on either side surfacing a mismatch; deliberately recorded rather than reconciled | [business logic](application/desktop_onboarding/desktop_onboarding.md) \| [tech](application/desktop_onboarding/desktop_onboarding_tech.md) |
| external_agent_access | REST + A2A surface for native clients (Desktop/Mobile) — agent discovery across personal agents and identity contacts (the `"app_mcp_route"` target type and its `/a2a/route/{id}/` endpoint are deleted as of Phase 5 of the channels & identity unification, along with the `AppAgentRoute` family it read; `target_type` is now `Literal["agent", "identity"]`, and discovery is two ordered sections, not three — Cinna Desktop and the separate `cinna-cli` repo both consumed the route target and will need a follow-up); A2A chat with both remaining target types; session metadata REST layer with thread-list restore; soft-hide; client attribution from JWT claims | [business logic](application/external_agent_access/external_agent_access.md) |
| cinna_mcp_descriptor | `cinna.mcp` descriptor on the external A2A surface — published in `capabilities.extensions[]` on the A2A card (uri `urn:cinna:mcp`) and mirrored as a top-level `mcp` field on each discovery target; lets Cinna Desktop wrap any reachable agent as an emulated MCP tool without a second OAuth flow; shared `send_message` contract in `tool_contracts.py` prevents core/desktop drift; deterministic slug deconfliction across the user's full reachable set; identity contacts carry `mcp=null` | [business logic](application/external_agent_access/cinna_mcp_descriptor.md) \| [tech](application/external_agent_access/cinna_mcp_descriptor_tech.md) |
| system_notifications | Generic platform notification layer — typed catalog, per-user preferences (`user_notification_setting`), in-memory throttle (dedup + rate cap), failure-isolated dispatch; first type: `session_error` email to the agent owner when a session ends with an error; Settings → My profile → Notifications card | [business logic](application/system_notifications/system_notifications.md) \| [tech](application/system_notifications/system_notifications_tech.md) |
| app_sync | Zero-knowledge native-client data sync — opaque E2E-encrypted document store for Cinna Desktop (and future Mobile) personal data (notes, jobs, folders). Delta sync protocol with per-user gap-free seq cursor, LWW conflict resolution on cleartext metadata, tombstones, per-user quotas. Mandatory E2E encryption: UMK hierarchy, commit-then-reveal device pairing (blind server relay, grind-proof SAS, inbox-based auto-discovery), recovery key, optional passphrase. No server payload crypto — ciphertext stored verbatim. Phase 1 MVP: notes + jobs + folders; chats and job runs in Phase 2/3 | [business logic](application/app_sync/app_sync.md) \| [tech](application/app_sync/app_sync_tech.md) |

### knowledge

| Feature | Description | Docs |
|---------|-------------|------|
| knowledge_management | Admin-only Git-based knowledge sources, article indexing, vector search. Public sources auto-available to all users' agents | [business logic](application/knowledge_sources/knowledge_sources.md) \| [tech](application/knowledge_sources/knowledge_sources_tech.md) |

### sharing

> Note: `agent_sharing` (clone-based sharing) has been removed and replaced by the bundle/install model. The `docs/agents/agent_sharing/` directory has been deleted. <!-- nocheck -->


| Feature | Description | Docs |
|---------|-------------|------|
| guest_sharing | Token-based unauthenticated chat access to an agent install — disposable URLs with 4-digit security codes, expiration, lockout protection, optional env-panel access, and grant activation for authenticated users | [business logic](agents/guest_sharing/guest_sharing.md) \| [tech](agents/guest_sharing/guest_sharing_tech.md) |
| workspaces | Workspace isolation, entity separation, multi-workspace support | [business logic](application/user_workspaces/user_workspaces.md) |

### agentic_teams

| Feature | Description | Docs |
|---------|-------------|------|
| agentic_teams | Visual org-chart builder — users define named agentic teams, add agent nodes, and wire directed connections with handover prompts. Owner-only access, workspace-independent, MVP Blueprint phase. Sidebar switcher + Settings card + interactive React Flow chart with edit/view mode, auto-arrange (Dagre), and bulk position persistence. Teams define a `task_prefix` (e.g., "HR") used when generating short-code IDs for team-scoped tasks; directed connections enforce subtask delegation topology. Connection Edit Dialog shows colored agent badges and supports AI-generated handover prompts via a Generate button. | [business logic](agents/agentic_teams/agentic_teams.md) \| [tech](agents/agentic_teams/agentic_teams_tech.md) |

### system

| Feature | Description | Docs |
|---------|-------------|------|
| status_repair | Cross-domain reconciler for rows abandoned in a transitional status (`AgentEnvironment.status` mid-activation/build, `Session.interaction_status` mid-stream/pending-delivery, `InputTask.status="in_progress"`, `ChannelTurnDelivery.role="draft"`) after a backend restart or crash cancels the fire-and-forget task that owned them (`asyncio.CancelledError` is a `BaseException`, so `except Exception` fallbacks never run). APScheduler sweep every 2 min, single-leader via Postgres advisory lock, four ordered passes (environments → sessions → input tasks → channel deliveries) sharing one per-tick in-memory handoff. Verifies live evidence (container probe, session/relay state) before ever writing a terminal status — never repairs on age alone; a liveness heartbeat (`AgentEnvironment.status_changed_at`, stamped by the lifecycle's sole status writers) distinguishes a slow-but-alive operation from a dead one. | [business logic](system/status_repair/status_repair.md) \| [tech](system/status_repair/status_repair_tech.md) |

### infrastructure

| Feature | Description | Docs |
|---------|-------------|------|
| nginx_setup | Nginx reverse-proxy location blocks required by the platform — `/api/`, `/mcp/`, `/ws/`, and origin-root `/.well-known/*` URIs (MCP OAuth, Cinna Desktop) — with feature cross-references | [reference](infrastructure/nginx_setup.md) |

### development

| Feature | Description | Docs |
|---------|-------------|------|
| backend_patterns | SQLModel patterns, routes, services, CRUD, migrations | [reference](development/backend/backend_development_llm.md) |
| frontend_patterns | Component patterns, hooks, TanStack conventions | [reference](development/frontend/frontend_development_llm.md) |
| ui_ux_guidelines | UI composition rules: four user stories, placement thresholds, house patterns P1–P9, anti-patterns, review checklist and score, screenshot gate | [reference](development/frontend/ui_ux_guidelines.md) |
| user_selector_pattern | Shared user-picker (`UserAllowlistPicker`) + `GET /users/search` endpoint used by credential sharing, App MCP / identity assignments, and bundle access grants | [reference](development/frontend/user_selector_pattern.md) |
| ai_functions | LLM utility development, multi-provider cascade fallback | [reference](development/backend/ai_functions_development.md) |
| security | Credentials whitelist, encryption at rest, access control | [reference](development/security/security.md) <!-- nocheck --> |

---

## Architecture Overview

```
User ──→ Frontend (React) ──→ Backend API (FastAPI) ──→ Services ──→ PostgreSQL
                                      │
                                      ├──→ Docker Environments ──→ Agent SDK (Claude/OpenAI)
                                      ├──→ WebSocket (Socket.IO) ──→ Real-time Events
                                      ├──→ A2A Protocol ──→ External Agents
                                      ├──→ MCP Server ──→ External LLM Clients
                                      │       └──→ App MCP Server ──→ Stage 1 Router ──→ Stage 2 Identity Router
                                      └──→ Server Channels (Google Chat webhook, Email IMAP/SMTP poll) ──→ Channel Routing
```

---

*Last updated: 2026-08-26* <!-- channel message attachments: inbound file attachments for server channels -->
